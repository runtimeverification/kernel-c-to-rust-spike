-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [uvc_verify]
import Aeneas
open Aeneas Aeneas.Std Result ControlFlow Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can set the `maxRecDepth` value with the `-max-recdepth` CLI option -/
set_option maxRecDepth 2048

namespace uvc_verify

/-- [uvc_verify::UVC_MAX_FORMATS]
    Source: 'src/lib.rs', lines 28:0-28:38
    Visibility: public -/
@[global_simps, irreducible] def UVC_MAX_FORMATS : Std.Usize := 64#usize

/-- [uvc_verify::UVC_MAX_FRAMES]
    Source: 'src/lib.rs', lines 29:0-29:38
    Visibility: public -/
@[global_simps, irreducible] def UVC_MAX_FRAMES : Std.Usize := 256#usize

/-- [uvc_verify::UVC_MAX_INTERVALS]
    Source: 'src/lib.rs', lines 30:0-30:42
    Visibility: public -/
@[global_simps, irreducible] def UVC_MAX_INTERVALS : Std.Usize := 1024#usize

/-- [uvc_verify::UvcFormatOut]
    Source: 'src/lib.rs', lines 34:0-45:1
    Visibility: public -/
structure UvcFormatOut where
  type_ : Std.U8
  index : Std.U8
  bpp : Std.U8
  colorspace : Std.I32
  xfer_func : Std.I32
  ycbcr_enc : Std.I32
  fcc : Std.U32
  flags : Std.U32
  nframes : Std.U32
  frame_base : Std.I32

/-- [uvc_verify::{impl core::clone::Clone for uvc_verify::UvcFormatOut}::clone]:
    Source: 'src/lib.rs', lines 33:9-33:14
    Visibility: public -/
def UvcFormatOut.Insts.CoreCloneClone.clone
  (self : UvcFormatOut) : Result UvcFormatOut := do
  ok self

/-- Trait implementation: [uvc_verify::{impl core::clone::Clone for uvc_verify::UvcFormatOut}]
    Source: 'src/lib.rs', lines 33:9-33:14 -/
@[reducible]
def UvcFormatOut.Insts.CoreCloneClone : core.clone.Clone UvcFormatOut := {
  clone := UvcFormatOut.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [uvc_verify::{impl core::marker::Copy for uvc_verify::UvcFormatOut}]
    Source: 'src/lib.rs', lines 33:16-33:20 -/
@[reducible]
def UvcFormatOut.Insts.CoreMarkerCopy : core.marker.Copy UvcFormatOut := {
  cloneInst := UvcFormatOut.Insts.CoreCloneClone
}

/-- Trait implementation: [uvc_verify::{impl core::marker::StructuralPartialEq for uvc_verify::UvcFormatOut}]
    Source: 'src/lib.rs', lines 33:22-33:31 -/
@[reducible]
def UvcFormatOut.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq UvcFormatOut := {
}

/-- [uvc_verify::{impl core::cmp::PartialEq<uvc_verify::UvcFormatOut> for uvc_verify::UvcFormatOut}::eq]:
    Source: 'src/lib.rs', lines 33:22-33:31
    Visibility: public -/
def UvcFormatOut.Insts.CoreCmpPartialEqUvcFormatOut.eq
  (self : UvcFormatOut) (other : UvcFormatOut) : Result Bool := do
  if self.type_ = other.type_
  then
    if self.index = other.index
    then
      if self.bpp = other.bpp
      then
        if self.colorspace = other.colorspace
        then
          if self.xfer_func = other.xfer_func
          then
            if self.ycbcr_enc = other.ycbcr_enc
            then
              if self.fcc = other.fcc
              then
                if self.flags = other.flags
                then
                  if self.nframes = other.nframes
                  then ok (self.frame_base = other.frame_base)
                  else ok false
                else ok false
              else ok false
            else ok false
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

/-- Trait implementation: [uvc_verify::{impl core::cmp::PartialEq<uvc_verify::UvcFormatOut> for uvc_verify::UvcFormatOut}]
    Source: 'src/lib.rs', lines 33:22-33:31 -/
@[reducible]
def UvcFormatOut.Insts.CoreCmpPartialEqUvcFormatOut : core.cmp.PartialEq
  UvcFormatOut UvcFormatOut := {
  eq := UvcFormatOut.Insts.CoreCmpPartialEqUvcFormatOut.eq
}

/-- [uvc_verify::{impl core::fmt::Debug for uvc_verify::UvcFormatOut}::fmt]:
    Source: 'src/lib.rs', lines 33:33-33:38
    Visibility: public -/
def UvcFormatOut.Insts.CoreFmtDebug.fmt
  (self : UvcFormatOut) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ core.fmt.DebugU8 self.type_
  let dyn1 := Dyn.mk _ core.fmt.DebugU8 self.index
  let dyn2 := Dyn.mk _ core.fmt.DebugU8 self.bpp
  let dyn3 := Dyn.mk _ core.fmt.DebugI32 self.colorspace
  let dyn4 := Dyn.mk _ core.fmt.DebugI32 self.xfer_func
  let dyn5 := Dyn.mk _ core.fmt.DebugI32 self.ycbcr_enc
  let dyn6 := Dyn.mk _ core.fmt.DebugU32 self.fcc
  let dyn7 := Dyn.mk _ core.fmt.DebugU32 self.flags
  let dyn8 := Dyn.mk _ core.fmt.DebugU32 self.nframes
  let dyn9 := Dyn.mk _ (core.fmt.DebugShared core.fmt.DebugI32) self.frame_base
  let values :=
    Array.to_slice
      (Array.make 10#usize [
        dyn, dyn1, dyn2, dyn3, dyn4, dyn5, dyn6, dyn7, dyn8, dyn9
        ])
  let s ←
    lift (Array.to_slice
      (Array.make 10#usize [
        toStr "type_", toStr "index", toStr "bpp", toStr "colorspace", toStr
        "xfer_func", toStr "ycbcr_enc", toStr "fcc", toStr "flags", toStr
        "nframes", toStr "frame_base"
        ]))
  core.fmt.Formatter.debug_struct_fields_finish f (toStr "UvcFormatOut") s
    values

/-- Trait implementation: [uvc_verify::{impl core::fmt::Debug for uvc_verify::UvcFormatOut}]
    Source: 'src/lib.rs', lines 33:33-33:38 -/
@[reducible]
def UvcFormatOut.Insts.CoreFmtDebug : core.fmt.Debug UvcFormatOut := {
  fmt := UvcFormatOut.Insts.CoreFmtDebug.fmt
}

/-- [uvc_verify::UvcFrameOut]
    Source: 'src/lib.rs', lines 49:0-60:1
    Visibility: public -/
structure UvcFrameOut where
  b_frame_index : Std.U8
  bm_capabilities : Std.U8
  w_width : Std.U16
  w_height : Std.U16
  dw_min_bit_rate : Std.U32
  dw_max_bit_rate : Std.U32
  dw_max_video_frame_buffer_size : Std.U32
  b_frame_interval_type : Std.U8
  dw_default_frame_interval : Std.U32
  interval_base : Std.I32

/-- [uvc_verify::{impl core::clone::Clone for uvc_verify::UvcFrameOut}::clone]:
    Source: 'src/lib.rs', lines 48:9-48:14
    Visibility: public -/
def UvcFrameOut.Insts.CoreCloneClone.clone
  (self : UvcFrameOut) : Result UvcFrameOut := do
  ok self

/-- Trait implementation: [uvc_verify::{impl core::clone::Clone for uvc_verify::UvcFrameOut}]
    Source: 'src/lib.rs', lines 48:9-48:14 -/
@[reducible]
def UvcFrameOut.Insts.CoreCloneClone : core.clone.Clone UvcFrameOut := {
  clone := UvcFrameOut.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [uvc_verify::{impl core::marker::Copy for uvc_verify::UvcFrameOut}]
    Source: 'src/lib.rs', lines 48:16-48:20 -/
@[reducible]
def UvcFrameOut.Insts.CoreMarkerCopy : core.marker.Copy UvcFrameOut := {
  cloneInst := UvcFrameOut.Insts.CoreCloneClone
}

/-- Trait implementation: [uvc_verify::{impl core::marker::StructuralPartialEq for uvc_verify::UvcFrameOut}]
    Source: 'src/lib.rs', lines 48:22-48:31 -/
@[reducible]
def UvcFrameOut.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq UvcFrameOut := {
}

/-- [uvc_verify::{impl core::cmp::PartialEq<uvc_verify::UvcFrameOut> for uvc_verify::UvcFrameOut}::eq]:
    Source: 'src/lib.rs', lines 48:22-48:31
    Visibility: public -/
def UvcFrameOut.Insts.CoreCmpPartialEqUvcFrameOut.eq
  (self : UvcFrameOut) (other : UvcFrameOut) : Result Bool := do
  if self.b_frame_index = other.b_frame_index
  then
    if self.bm_capabilities = other.bm_capabilities
    then
      if self.w_width = other.w_width
      then
        if self.w_height = other.w_height
        then
          if self.dw_min_bit_rate = other.dw_min_bit_rate
          then
            if self.dw_max_bit_rate = other.dw_max_bit_rate
            then
              if self.dw_max_video_frame_buffer_size =
                other.dw_max_video_frame_buffer_size
              then
                if self.b_frame_interval_type = other.b_frame_interval_type
                then
                  if self.dw_default_frame_interval =
                    other.dw_default_frame_interval
                  then ok (self.interval_base = other.interval_base)
                  else ok false
                else ok false
              else ok false
            else ok false
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

/-- Trait implementation: [uvc_verify::{impl core::cmp::PartialEq<uvc_verify::UvcFrameOut> for uvc_verify::UvcFrameOut}]
    Source: 'src/lib.rs', lines 48:22-48:31 -/
@[reducible]
def UvcFrameOut.Insts.CoreCmpPartialEqUvcFrameOut : core.cmp.PartialEq
  UvcFrameOut UvcFrameOut := {
  eq := UvcFrameOut.Insts.CoreCmpPartialEqUvcFrameOut.eq
}

/-- [uvc_verify::{impl core::fmt::Debug for uvc_verify::UvcFrameOut}::fmt]:
    Source: 'src/lib.rs', lines 48:33-48:38
    Visibility: public -/
def UvcFrameOut.Insts.CoreFmtDebug.fmt
  (self : UvcFrameOut) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ core.fmt.DebugU8 self.b_frame_index
  let dyn1 := Dyn.mk _ core.fmt.DebugU8 self.bm_capabilities
  let dyn2 := Dyn.mk _ core.fmt.DebugU16 self.w_width
  let dyn3 := Dyn.mk _ core.fmt.DebugU16 self.w_height
  let dyn4 := Dyn.mk _ core.fmt.DebugU32 self.dw_min_bit_rate
  let dyn5 := Dyn.mk _ core.fmt.DebugU32 self.dw_max_bit_rate
  let dyn6 := Dyn.mk _ core.fmt.DebugU32 self.dw_max_video_frame_buffer_size
  let dyn7 := Dyn.mk _ core.fmt.DebugU8 self.b_frame_interval_type
  let dyn8 := Dyn.mk _ core.fmt.DebugU32 self.dw_default_frame_interval
  let dyn9 :=
    Dyn.mk _ (core.fmt.DebugShared core.fmt.DebugI32) self.interval_base
  let values :=
    Array.to_slice
      (Array.make 10#usize [
        dyn, dyn1, dyn2, dyn3, dyn4, dyn5, dyn6, dyn7, dyn8, dyn9
        ])
  let s ←
    lift (Array.to_slice
      (Array.make 10#usize [
        toStr "b_frame_index", toStr "bm_capabilities", toStr "w_width", toStr
        "w_height", toStr "dw_min_bit_rate", toStr "dw_max_bit_rate", toStr
        "dw_max_video_frame_buffer_size", toStr "b_frame_interval_type", toStr
        "dw_default_frame_interval", toStr "interval_base"
        ]))
  core.fmt.Formatter.debug_struct_fields_finish f (toStr "UvcFrameOut") s
    values

/-- Trait implementation: [uvc_verify::{impl core::fmt::Debug for uvc_verify::UvcFrameOut}]
    Source: 'src/lib.rs', lines 48:33-48:38 -/
@[reducible]
def UvcFrameOut.Insts.CoreFmtDebug : core.fmt.Debug UvcFrameOut := {
  fmt := UvcFrameOut.Insts.CoreFmtDebug.fmt
}

/-- [uvc_verify::UvcParseResult]
    Source: 'src/lib.rs', lines 67:0-78:1
    Visibility: public -/
structure UvcParseResult where
  ret : Std.I32
  nformats_alloc : Std.U32
  nframes_alloc : Std.U32
  nintervals_alloc : Std.U32
  nformats : Std.U32
  nframes : Std.U32
  nintervals : Std.U32
  formats : Array UvcFormatOut 64#usize
  frames : Array UvcFrameOut 256#usize
  intervals : Array Std.U32 1024#usize

/-- [uvc_verify::USB_DT_CS_INTERFACE]
    Source: 'src/lib.rs', lines 84:0-84:37 -/
@[global_simps, irreducible] def USB_DT_CS_INTERFACE : Std.U8 := 36#u8

/-- [uvc_verify::UVC_VS_FORMAT_UNCOMPRESSED]
    Source: 'src/lib.rs', lines 86:0-86:44 -/
@[global_simps, irreducible] def UVC_VS_FORMAT_UNCOMPRESSED : Std.U8 := 4#u8

/-- [uvc_verify::UVC_VS_FRAME_UNCOMPRESSED]
    Source: 'src/lib.rs', lines 87:0-87:43 -/
@[global_simps, irreducible] def UVC_VS_FRAME_UNCOMPRESSED : Std.U8 := 5#u8

/-- [uvc_verify::UVC_VS_FORMAT_MJPEG]
    Source: 'src/lib.rs', lines 88:0-88:37 -/
@[global_simps, irreducible] def UVC_VS_FORMAT_MJPEG : Std.U8 := 6#u8

/-- [uvc_verify::UVC_VS_FRAME_MJPEG]
    Source: 'src/lib.rs', lines 89:0-89:36 -/
@[global_simps, irreducible] def UVC_VS_FRAME_MJPEG : Std.U8 := 7#u8

/-- [uvc_verify::UVC_VS_FORMAT_MPEG2TS]
    Source: 'src/lib.rs', lines 90:0-90:39 -/
@[global_simps, irreducible] def UVC_VS_FORMAT_MPEG2TS : Std.U8 := 10#u8

/-- [uvc_verify::UVC_VS_FORMAT_DV]
    Source: 'src/lib.rs', lines 91:0-91:34 -/
@[global_simps, irreducible] def UVC_VS_FORMAT_DV : Std.U8 := 12#u8

/-- [uvc_verify::UVC_VS_COLORFORMAT]
    Source: 'src/lib.rs', lines 92:0-92:36 -/
@[global_simps, irreducible] def UVC_VS_COLORFORMAT : Std.U8 := 13#u8

/-- [uvc_verify::UVC_VS_FORMAT_FRAME_BASED]
    Source: 'src/lib.rs', lines 93:0-93:43 -/
@[global_simps, irreducible] def UVC_VS_FORMAT_FRAME_BASED : Std.U8 := 16#u8

/-- [uvc_verify::UVC_VS_FRAME_FRAME_BASED]
    Source: 'src/lib.rs', lines 94:0-94:42 -/
@[global_simps, irreducible] def UVC_VS_FRAME_FRAME_BASED : Std.U8 := 17#u8

/-- [uvc_verify::UVC_VS_FORMAT_STREAM_BASED]
    Source: 'src/lib.rs', lines 95:0-95:44 -/
@[global_simps, irreducible] def UVC_VS_FORMAT_STREAM_BASED : Std.U8 := 18#u8

/-- [uvc_verify::UVC_VS_STILL_IMAGE_FRAME]
    Source: 'src/lib.rs', lines 96:0-96:42 -/
@[global_simps, irreducible] def UVC_VS_STILL_IMAGE_FRAME : Std.U8 := 3#u8

/-- [uvc_verify::UVC_QUIRK_RESTRICT_FRAME_RATE]
    Source: 'src/lib.rs', lines 98:0-98:55 -/
@[global_simps, irreducible]
def UVC_QUIRK_RESTRICT_FRAME_RATE : Std.U32 := 512#u32

/-- [uvc_verify::UVC_QUIRK_FORCE_Y8]
    Source: 'src/lib.rs', lines 99:0-99:44 -/
@[global_simps, irreducible] def UVC_QUIRK_FORCE_Y8 : Std.U32 := 2048#u32

/-- [uvc_verify::UVC_QUIRK_FORCE_BPP]
    Source: 'src/lib.rs', lines 100:0-100:45 -/
@[global_simps, irreducible] def UVC_QUIRK_FORCE_BPP : Std.U32 := 4096#u32

/-- [uvc_verify::UVC_FMT_FLAG_COMPRESSED]
    Source: 'src/lib.rs', lines 101:0-101:49 -/
@[global_simps, irreducible] def UVC_FMT_FLAG_COMPRESSED : Std.U32 := 1#u32

/-- [uvc_verify::UVC_FMT_FLAG_STREAM]
    Source: 'src/lib.rs', lines 102:0-102:45 -/
@[global_simps, irreducible] def UVC_FMT_FLAG_STREAM : Std.U32 := 2#u32

/-- [uvc_verify::V4L2_COLORSPACE_SMPTE170M]
    Source: 'src/lib.rs', lines 105:0-105:41 -/
@[global_simps, irreducible] def V4L2_COLORSPACE_SMPTE170M : Std.I32 := 1#i32

/-- [uvc_verify::V4L2_COLORSPACE_SMPTE240M]
    Source: 'src/lib.rs', lines 106:0-106:41 -/
@[global_simps, irreducible] def V4L2_COLORSPACE_SMPTE240M : Std.I32 := 2#i32

/-- [uvc_verify::V4L2_COLORSPACE_470_SYSTEM_M]
    Source: 'src/lib.rs', lines 107:0-107:44 -/
@[global_simps, irreducible]
def V4L2_COLORSPACE_470_SYSTEM_M : Std.I32 := 5#i32

/-- [uvc_verify::V4L2_COLORSPACE_470_SYSTEM_BG]
    Source: 'src/lib.rs', lines 108:0-108:45 -/
@[global_simps, irreducible]
def V4L2_COLORSPACE_470_SYSTEM_BG : Std.I32 := 6#i32

/-- [uvc_verify::V4L2_COLORSPACE_SRGB]
    Source: 'src/lib.rs', lines 109:0-109:36 -/
@[global_simps, irreducible] def V4L2_COLORSPACE_SRGB : Std.I32 := 8#i32

/-- [uvc_verify::V4L2_XFER_FUNC_DEFAULT]
    Source: 'src/lib.rs', lines 111:0-111:38 -/
@[global_simps, irreducible] def V4L2_XFER_FUNC_DEFAULT : Std.I32 := 0#i32

/-- [uvc_verify::V4L2_XFER_FUNC_709]
    Source: 'src/lib.rs', lines 112:0-112:34 -/
@[global_simps, irreducible] def V4L2_XFER_FUNC_709 : Std.I32 := 1#i32

/-- [uvc_verify::V4L2_XFER_FUNC_SRGB]
    Source: 'src/lib.rs', lines 113:0-113:35 -/
@[global_simps, irreducible] def V4L2_XFER_FUNC_SRGB : Std.I32 := 2#i32

/-- [uvc_verify::V4L2_XFER_FUNC_SMPTE240M]
    Source: 'src/lib.rs', lines 114:0-114:40 -/
@[global_simps, irreducible] def V4L2_XFER_FUNC_SMPTE240M : Std.I32 := 4#i32

/-- [uvc_verify::V4L2_XFER_FUNC_NONE]
    Source: 'src/lib.rs', lines 115:0-115:35 -/
@[global_simps, irreducible] def V4L2_XFER_FUNC_NONE : Std.I32 := 5#i32

/-- [uvc_verify::V4L2_YCBCR_ENC_DEFAULT]
    Source: 'src/lib.rs', lines 117:0-117:38 -/
@[global_simps, irreducible] def V4L2_YCBCR_ENC_DEFAULT : Std.I32 := 0#i32

/-- [uvc_verify::V4L2_YCBCR_ENC_601]
    Source: 'src/lib.rs', lines 118:0-118:34 -/
@[global_simps, irreducible] def V4L2_YCBCR_ENC_601 : Std.I32 := 1#i32

/-- [uvc_verify::V4L2_YCBCR_ENC_709]
    Source: 'src/lib.rs', lines 119:0-119:34 -/
@[global_simps, irreducible] def V4L2_YCBCR_ENC_709 : Std.I32 := 2#i32

/-- [uvc_verify::V4L2_YCBCR_ENC_SMPTE240M]
    Source: 'src/lib.rs', lines 120:0-120:40 -/
@[global_simps, irreducible] def V4L2_YCBCR_ENC_SMPTE240M : Std.I32 := 8#i32

/-- [uvc_verify::EINVAL]
    Source: 'src/lib.rs', lines 122:0-122:23 -/
@[global_simps, irreducible] def EINVAL : Std.I32 := 22#i32

/-- [uvc_verify::ENOMEM]
    Source: 'src/lib.rs', lines 123:0-123:23 -/
@[global_simps, irreducible] def ENOMEM : Std.I32 := 12#i32

/-- [uvc_verify::fourcc]:
    Source: 'src/lib.rs', lines 125:0-127:1 -/
def fourcc
  (a : Std.U8) (b : Std.U8) (c : Std.U8) (d : Std.U8) : Result Std.U32 := do
  let i ← lift (UScalar.cast .U32 a)
  let i1 ← lift (UScalar.cast .U32 b)
  let i2 ← i1 <<< 8#i32
  let i3 ← lift (i ||| i2)
  let i4 ← lift (UScalar.cast .U32 c)
  let i5 ← i4 <<< 16#i32
  let i6 ← lift (i3 ||| i5)
  let i7 ← lift (UScalar.cast .U32 d)
  let i8 ← i7 <<< 24#i32
  ok (i6 ||| i8)

/-- [uvc_verify::V4L2_PIX_FMT_YUYV]
    Source: 'src/lib.rs', lines 129:0-129:62 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_YUYV : Result Std.U32 := fourcc 89#u8 85#u8 89#u8 86#u8

/-- [uvc_verify::V4L2_PIX_FMT_NV12]
    Source: 'src/lib.rs', lines 130:0-130:62 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_NV12 : Result Std.U32 := fourcc 78#u8 86#u8 49#u8 50#u8

/-- [uvc_verify::V4L2_PIX_FMT_MJPEG]
    Source: 'src/lib.rs', lines 131:0-131:63 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_MJPEG : Result Std.U32 := fourcc 77#u8 74#u8 80#u8 71#u8

/-- [uvc_verify::V4L2_PIX_FMT_YVU420]
    Source: 'src/lib.rs', lines 132:0-132:64 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_YVU420 : Result Std.U32 := fourcc 89#u8 86#u8 49#u8 50#u8

/-- [uvc_verify::V4L2_PIX_FMT_YUV420]
    Source: 'src/lib.rs', lines 133:0-133:64 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_YUV420 : Result Std.U32 := fourcc 89#u8 85#u8 49#u8 50#u8

/-- [uvc_verify::V4L2_PIX_FMT_M420]
    Source: 'src/lib.rs', lines 134:0-134:62 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_M420 : Result Std.U32 := fourcc 77#u8 52#u8 50#u8 48#u8

/-- [uvc_verify::V4L2_PIX_FMT_P010]
    Source: 'src/lib.rs', lines 135:0-135:62 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_P010 : Result Std.U32 := fourcc 80#u8 48#u8 49#u8 48#u8

/-- [uvc_verify::V4L2_PIX_FMT_UYVY]
    Source: 'src/lib.rs', lines 136:0-136:62 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_UYVY : Result Std.U32 := fourcc 85#u8 89#u8 86#u8 89#u8

/-- [uvc_verify::V4L2_PIX_FMT_GREY]
    Source: 'src/lib.rs', lines 137:0-137:62 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_GREY : Result Std.U32 := fourcc 71#u8 82#u8 69#u8 89#u8

/-- [uvc_verify::V4L2_PIX_FMT_Y10]
    Source: 'src/lib.rs', lines 138:0-138:61 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_Y10 : Result Std.U32 := fourcc 89#u8 49#u8 48#u8 32#u8

/-- [uvc_verify::V4L2_PIX_FMT_Y12]
    Source: 'src/lib.rs', lines 139:0-139:61 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_Y12 : Result Std.U32 := fourcc 89#u8 49#u8 50#u8 32#u8

/-- [uvc_verify::V4L2_PIX_FMT_Y16]
    Source: 'src/lib.rs', lines 140:0-140:61 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_Y16 : Result Std.U32 := fourcc 89#u8 49#u8 54#u8 32#u8

/-- [uvc_verify::V4L2_PIX_FMT_SBGGR8]
    Source: 'src/lib.rs', lines 141:0-141:64 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_SBGGR8 : Result Std.U32 := fourcc 66#u8 65#u8 56#u8 49#u8

/-- [uvc_verify::V4L2_PIX_FMT_SGBRG8]
    Source: 'src/lib.rs', lines 142:0-142:64 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_SGBRG8 : Result Std.U32 := fourcc 71#u8 66#u8 82#u8 71#u8

/-- [uvc_verify::V4L2_PIX_FMT_SGRBG8]
    Source: 'src/lib.rs', lines 143:0-143:64 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_SGRBG8 : Result Std.U32 := fourcc 71#u8 82#u8 66#u8 71#u8

/-- [uvc_verify::V4L2_PIX_FMT_SRGGB8]
    Source: 'src/lib.rs', lines 144:0-144:64 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_SRGGB8 : Result Std.U32 := fourcc 82#u8 71#u8 71#u8 66#u8

/-- [uvc_verify::V4L2_PIX_FMT_RGB565]
    Source: 'src/lib.rs', lines 145:0-145:64 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_RGB565 : Result Std.U32 := fourcc 82#u8 71#u8 66#u8 80#u8

/-- [uvc_verify::V4L2_PIX_FMT_BGR24]
    Source: 'src/lib.rs', lines 146:0-146:63 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_BGR24 : Result Std.U32 := fourcc 66#u8 71#u8 82#u8 51#u8

/-- [uvc_verify::V4L2_PIX_FMT_XBGR32]
    Source: 'src/lib.rs', lines 147:0-147:64 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_XBGR32 : Result Std.U32 := fourcc 88#u8 82#u8 50#u8 52#u8

/-- [uvc_verify::V4L2_PIX_FMT_H264]
    Source: 'src/lib.rs', lines 148:0-148:62 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_H264 : Result Std.U32 := fourcc 72#u8 50#u8 54#u8 52#u8

/-- [uvc_verify::V4L2_PIX_FMT_HEVC]
    Source: 'src/lib.rs', lines 149:0-149:62 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_HEVC : Result Std.U32 := fourcc 72#u8 69#u8 86#u8 67#u8

/-- [uvc_verify::V4L2_PIX_FMT_Y8I]
    Source: 'src/lib.rs', lines 150:0-150:61 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_Y8I : Result Std.U32 := fourcc 89#u8 56#u8 73#u8 32#u8

/-- [uvc_verify::V4L2_PIX_FMT_Y12I]
    Source: 'src/lib.rs', lines 151:0-151:62 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_Y12I : Result Std.U32 := fourcc 89#u8 49#u8 50#u8 73#u8

/-- [uvc_verify::V4L2_PIX_FMT_Y16I]
    Source: 'src/lib.rs', lines 152:0-152:62 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_Y16I : Result Std.U32 := fourcc 89#u8 49#u8 54#u8 73#u8

/-- [uvc_verify::V4L2_PIX_FMT_Z16]
    Source: 'src/lib.rs', lines 153:0-153:61 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_Z16 : Result Std.U32 := fourcc 90#u8 49#u8 54#u8 32#u8

/-- [uvc_verify::V4L2_PIX_FMT_SRGGB10P]
    Source: 'src/lib.rs', lines 154:0-154:66 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_SRGGB10P : Result Std.U32 := fourcc 112#u8 82#u8 65#u8 65#u8

/-- [uvc_verify::V4L2_PIX_FMT_SBGGR16]
    Source: 'src/lib.rs', lines 155:0-155:65 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_SBGGR16 : Result Std.U32 := fourcc 66#u8 89#u8 82#u8 50#u8

/-- [uvc_verify::V4L2_PIX_FMT_SGBRG16]
    Source: 'src/lib.rs', lines 156:0-156:65 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_SGBRG16 : Result Std.U32 := fourcc 71#u8 66#u8 49#u8 54#u8

/-- [uvc_verify::V4L2_PIX_FMT_SRGGB16]
    Source: 'src/lib.rs', lines 157:0-157:65 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_SRGGB16 : Result Std.U32 := fourcc 82#u8 71#u8 49#u8 54#u8

/-- [uvc_verify::V4L2_PIX_FMT_SGRBG16]
    Source: 'src/lib.rs', lines 158:0-158:65 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_SGRBG16 : Result Std.U32 := fourcc 71#u8 82#u8 49#u8 54#u8

/-- [uvc_verify::V4L2_PIX_FMT_DV]
    Source: 'src/lib.rs', lines 159:0-159:60 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_DV : Result Std.U32 := fourcc 100#u8 118#u8 115#u8 100#u8

/-- [uvc_verify::V4L2_PIX_FMT_INZI]
    Source: 'src/lib.rs', lines 160:0-160:62 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_INZI : Result Std.U32 := fourcc 73#u8 78#u8 90#u8 73#u8

/-- [uvc_verify::V4L2_PIX_FMT_CNF4]
    Source: 'src/lib.rs', lines 161:0-161:62 -/
@[global_simps, irreducible]
def V4L2_PIX_FMT_CNF4 : Result Std.U32 := fourcc 67#u8 78#u8 70#u8 52#u8

/-- [uvc_verify::FormatDesc]
    Source: 'src/lib.rs', lines 171:0-174:1 -/
structure FormatDesc where
  guid : Array Std.U8 16#usize
  fcc : Std.U32

/-- [uvc_verify::{impl core::clone::Clone for uvc_verify::FormatDesc}::clone]:
    Source: 'src/lib.rs', lines 170:9-170:14
    Visibility: public -/
def FormatDesc.Insts.CoreCloneClone.clone
  (self : FormatDesc) : Result FormatDesc := do
  ok self

/-- Trait implementation: [uvc_verify::{impl core::clone::Clone for uvc_verify::FormatDesc}]
    Source: 'src/lib.rs', lines 170:9-170:14 -/
@[reducible]
def FormatDesc.Insts.CoreCloneClone : core.clone.Clone FormatDesc := {
  clone := FormatDesc.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [uvc_verify::{impl core::marker::Copy for uvc_verify::FormatDesc}]
    Source: 'src/lib.rs', lines 170:16-170:20 -/
@[reducible]
def FormatDesc.Insts.CoreMarkerCopy : core.marker.Copy FormatDesc := {
  cloneInst := FormatDesc.Insts.CoreCloneClone
}

/-- [uvc_verify::UVC_FMTS]
    Source: 'src/lib.rs', lines 183:0-225:2 -/
@[global_simps, irreducible]
def UVC_FMTS : Result (Array FormatDesc 41#usize) := do
  let i ← V4L2_PIX_FMT_YUYV
  let i1 ← V4L2_PIX_FMT_NV12
  let i2 ← V4L2_PIX_FMT_MJPEG
  let i3 ← V4L2_PIX_FMT_YVU420
  let i4 ← V4L2_PIX_FMT_YUV420
  let i5 ← V4L2_PIX_FMT_M420
  let i6 ← V4L2_PIX_FMT_P010
  let i7 ← V4L2_PIX_FMT_UYVY
  let i8 ← V4L2_PIX_FMT_GREY
  let i9 ← V4L2_PIX_FMT_Y10
  let i10 ← V4L2_PIX_FMT_Y12
  let i11 ← V4L2_PIX_FMT_Y16
  let i12 ← V4L2_PIX_FMT_SBGGR8
  let i13 ← V4L2_PIX_FMT_SGBRG8
  let i14 ← V4L2_PIX_FMT_SGRBG8
  let i15 ← V4L2_PIX_FMT_SRGGB8
  let i16 ← V4L2_PIX_FMT_RGB565
  let i17 ← V4L2_PIX_FMT_BGR24
  let i18 ← V4L2_PIX_FMT_XBGR32
  let i19 ← V4L2_PIX_FMT_H264
  let i20 ← V4L2_PIX_FMT_HEVC
  let i21 ← V4L2_PIX_FMT_Y8I
  let i22 ← V4L2_PIX_FMT_Y12I
  let i23 ← V4L2_PIX_FMT_Y16I
  let i24 ← V4L2_PIX_FMT_Z16
  let i25 ← V4L2_PIX_FMT_SRGGB10P
  let i26 ← V4L2_PIX_FMT_SBGGR16
  let i27 ← V4L2_PIX_FMT_SGBRG16
  let i28 ← V4L2_PIX_FMT_SRGGB16
  let i29 ← V4L2_PIX_FMT_SGRBG16
  let i30 ← V4L2_PIX_FMT_INZI
  let i31 ← V4L2_PIX_FMT_CNF4
  ok
    (Array.make 41#usize [
      {
        guid :=
          (Array.make 16#usize [
            89#u8, 85#u8, 89#u8, 50#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i
      },
      {
        guid :=
          (Array.make 16#usize [
            89#u8, 85#u8, 89#u8, 50#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 0#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i
      },
      {
        guid :=
          (Array.make 16#usize [
            78#u8, 86#u8, 49#u8, 50#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i1
      },
      {
        guid :=
          (Array.make 16#usize [
            77#u8, 74#u8, 80#u8, 71#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i2
      },
      {
        guid :=
          (Array.make 16#usize [
            89#u8, 86#u8, 49#u8, 50#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i3
      },
      {
        guid :=
          (Array.make 16#usize [
            73#u8, 52#u8, 50#u8, 48#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i4
      },
      {
        guid :=
          (Array.make 16#usize [
            77#u8, 52#u8, 50#u8, 48#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i5
      },
      {
        guid :=
          (Array.make 16#usize [
            80#u8, 48#u8, 49#u8, 48#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i6
      },
      {
        guid :=
          (Array.make 16#usize [
            85#u8, 89#u8, 86#u8, 89#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i7
      },
      {
        guid :=
          (Array.make 16#usize [
            89#u8, 56#u8, 48#u8, 48#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i8
      },
      {
        guid :=
          (Array.make 16#usize [
            89#u8, 56#u8, 32#u8, 32#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i8
      },
      {
        guid :=
          (Array.make 16#usize [
            50#u8, 0#u8, 0#u8, 0#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i8
      },
      {
        guid :=
          (Array.make 16#usize [
            50#u8, 0#u8, 0#u8, 0#u8, 2#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i8
      },
      {
        guid :=
          (Array.make 16#usize [
            89#u8, 49#u8, 48#u8, 32#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i9
      },
      {
        guid :=
          (Array.make 16#usize [
            89#u8, 49#u8, 50#u8, 32#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i10
      },
      {
        guid :=
          (Array.make 16#usize [
            89#u8, 49#u8, 54#u8, 32#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i11
      },
      {
        guid :=
          (Array.make 16#usize [
            66#u8, 89#u8, 56#u8, 32#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i12
      },
      {
        guid :=
          (Array.make 16#usize [
            66#u8, 65#u8, 56#u8, 49#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i12
      },
      {
        guid :=
          (Array.make 16#usize [
            71#u8, 66#u8, 82#u8, 71#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i13
      },
      {
        guid :=
          (Array.make 16#usize [
            71#u8, 82#u8, 66#u8, 71#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i14
      },
      {
        guid :=
          (Array.make 16#usize [
            82#u8, 71#u8, 71#u8, 66#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i15
      },
      {
        guid :=
          (Array.make 16#usize [
            82#u8, 71#u8, 66#u8, 80#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i16
      },
      {
        guid :=
          (Array.make 16#usize [
            123#u8, 235#u8, 54#u8, 228#u8, 79#u8, 82#u8, 206#u8, 17#u8, 159#u8,
            83#u8, 0#u8, 32#u8, 175#u8, 11#u8, 167#u8, 112#u8
            ]),
        fcc := i16
      },
      {
        guid :=
          (Array.make 16#usize [
            125#u8, 235#u8, 54#u8, 228#u8, 79#u8, 82#u8, 206#u8, 17#u8, 159#u8,
            83#u8, 0#u8, 32#u8, 175#u8, 11#u8, 167#u8, 112#u8
            ]),
        fcc := i17
      },
      {
        guid :=
          (Array.make 16#usize [
            126#u8, 235#u8, 54#u8, 228#u8, 79#u8, 82#u8, 206#u8, 17#u8, 159#u8,
            83#u8, 0#u8, 32#u8, 175#u8, 11#u8, 167#u8, 112#u8
            ]),
        fcc := i18
      },
      {
        guid :=
          (Array.make 16#usize [
            72#u8, 50#u8, 54#u8, 52#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i19
      },
      {
        guid :=
          (Array.make 16#usize [
            72#u8, 50#u8, 54#u8, 53#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i20
      },
      {
        guid :=
          (Array.make 16#usize [
            89#u8, 56#u8, 73#u8, 32#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i21
      },
      {
        guid :=
          (Array.make 16#usize [
            89#u8, 49#u8, 50#u8, 73#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i22
      },
      {
        guid :=
          (Array.make 16#usize [
            89#u8, 49#u8, 54#u8, 73#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i23
      },
      {
        guid :=
          (Array.make 16#usize [
            90#u8, 49#u8, 54#u8, 32#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i24
      },
      {
        guid :=
          (Array.make 16#usize [
            82#u8, 87#u8, 49#u8, 48#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i25
      },
      {
        guid :=
          (Array.make 16#usize [
            66#u8, 71#u8, 49#u8, 54#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i26
      },
      {
        guid :=
          (Array.make 16#usize [
            71#u8, 66#u8, 49#u8, 54#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i27
      },
      {
        guid :=
          (Array.make 16#usize [
            82#u8, 71#u8, 49#u8, 54#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i28
      },
      {
        guid :=
          (Array.make 16#usize [
            71#u8, 82#u8, 49#u8, 54#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i29
      },
      {
        guid :=
          (Array.make 16#usize [
            73#u8, 78#u8, 86#u8, 90#u8, 144#u8, 45#u8, 88#u8, 74#u8, 146#u8,
            11#u8, 119#u8, 63#u8, 31#u8, 44#u8, 85#u8, 107#u8
            ]),
        fcc := i24
      },
      {
        guid :=
          (Array.make 16#usize [
            73#u8, 78#u8, 86#u8, 73#u8, 219#u8, 87#u8, 73#u8, 94#u8, 142#u8,
            63#u8, 244#u8, 121#u8, 83#u8, 43#u8, 148#u8, 111#u8
            ]),
        fcc := i9
      },
      {
        guid :=
          (Array.make 16#usize [
            73#u8, 78#u8, 90#u8, 73#u8, 102#u8, 26#u8, 66#u8, 162#u8, 144#u8,
            101#u8, 208#u8, 24#u8, 20#u8, 168#u8, 239#u8, 138#u8
            ]),
        fcc := i30
      },
      {
        guid :=
          (Array.make 16#usize [
            67#u8, 32#u8, 32#u8, 32#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i31
      },
      {
        guid :=
          (Array.make 16#usize [
            72#u8, 69#u8, 86#u8, 67#u8, 0#u8, 0#u8, 16#u8, 0#u8, 128#u8, 0#u8,
            0#u8, 170#u8, 0#u8, 56#u8, 155#u8, 113#u8
            ]),
        fcc := i20
      }
      ])

/-- [uvc_verify::guid_eq16]: loop body 0:
    Source: 'src/lib.rs', lines 243:4-248:5 -/
@[rust_loop_body]
def guid_eq16_loop.body
  (a : Array Std.U8 16#usize) (b : Slice Std.U8) (j : Std.Usize)
  (matched : Bool) :
  Result (ControlFlow (Std.Usize × Bool) Bool)
  := do
  if j < 16#usize
  then
    let i ← Array.index_usize a j
    let i1 ← Slice.index_usize b j
    let matched1 ← if i != i1
                     then ok false
                     else ok matched
    let j1 ← j + 1#usize
    ok (cont (j1, matched1))
  else ok (done matched)

/-- [uvc_verify::guid_eq16]: loop 0:
    Source: 'src/lib.rs', lines 243:4-248:5 -/
@[rust_loop]
def guid_eq16_loop
  (a : Array Std.U8 16#usize) (b : Slice Std.U8) (j : Std.Usize)
  (matched : Bool) :
  Result Bool
  := do
  loop
    (fun (j1, matched1) => guid_eq16_loop.body a b j1 matched1)
    (j, matched)

/-- [uvc_verify::guid_eq16]:
    Source: 'src/lib.rs', lines 240:0-250:1 -/
@[reducible]
def guid_eq16
  (a : Array Std.U8 16#usize) (b : Slice Std.U8) : Result Bool := do
  guid_eq16_loop a b 0#usize true

/-- [uvc_verify::uvc_format_by_guid]: loop body 0:
    Source: 'src/lib.rs', lines 256:4-264:1 -/
@[rust_loop_body]
def uvc_format_by_guid_loop.body
  (guid : Slice Std.U8) (i : Std.Usize) :
  Result (ControlFlow Std.Usize (Option Std.U32))
  := do
  let a ← UVC_FMTS
  let s ← lift (Array.to_slice a)
  let i1 := Slice.len s
  if i < i1
  then
    let fd ← Array.index_usize a i
    let b ← guid_eq16 fd.guid guid
    if b
    then ok (done (some fd.fcc))
    else let i2 ← i + 1#usize
         ok (cont i2)
  else ok (done none)

/-- [uvc_verify::uvc_format_by_guid]: loop 0:
    Source: 'src/lib.rs', lines 256:4-264:1 -/
@[rust_loop]
def uvc_format_by_guid_loop
  (guid : Slice Std.U8) (i : Std.Usize) : Result (Option Std.U32) := do
  loop
    (fun i1 => uvc_format_by_guid_loop.body guid i1)
    i

/-- [uvc_verify::uvc_format_by_guid]:
    Source: 'src/lib.rs', lines 252:0-264:1 -/
@[reducible]
def uvc_format_by_guid (guid : Slice Std.U8) : Result (Option Std.U32) := do
  uvc_format_by_guid_loop guid 0#usize

/-- [uvc_verify::uvc_colorspace::COLORPRIMARIES]
    Source: 'src/lib.rs', lines 271:4-278:6 -/
@[global_simps, irreducible]
def uvc_colorspace.COLORPRIMARIES : Array Std.I32 6#usize :=
  Array.make 6#usize [
    V4L2_COLORSPACE_SRGB, V4L2_COLORSPACE_SRGB, V4L2_COLORSPACE_470_SYSTEM_M,
    V4L2_COLORSPACE_470_SYSTEM_BG, V4L2_COLORSPACE_SMPTE170M,
    V4L2_COLORSPACE_SMPTE240M
    ]

/-- [uvc_verify::uvc_colorspace]:
    Source: 'src/lib.rs', lines 270:0-283:1 -/
def uvc_colorspace (primaries : Std.U8) : Result Std.I32 := do
  let s ← lift (Array.to_slice uvc_colorspace.COLORPRIMARIES)
  let i ← lift (UScalar.cast .Usize primaries)
  let o ←
    core.slice.Slice.get (core.slice.index.SliceIndexUsizeSlice Std.I32) s i
  match o with
  | none => ok V4L2_COLORSPACE_SRGB
  | some c => ok c

/-- [uvc_verify::uvc_xfer_func::XFER_FUNCS]
    Source: 'src/lib.rs', lines 286:4-295:6 -/
@[global_simps, irreducible]
def uvc_xfer_func.XFER_FUNCS : Array Std.I32 8#usize :=
  Array.make 8#usize [
    V4L2_XFER_FUNC_DEFAULT, V4L2_XFER_FUNC_709, V4L2_XFER_FUNC_709,
    V4L2_XFER_FUNC_709, V4L2_XFER_FUNC_709, V4L2_XFER_FUNC_SMPTE240M,
    V4L2_XFER_FUNC_NONE, V4L2_XFER_FUNC_SRGB
    ]

/-- [uvc_verify::uvc_xfer_func]:
    Source: 'src/lib.rs', lines 285:0-300:1 -/
def uvc_xfer_func (transfer_characteristics : Std.U8) : Result Std.I32 := do
  let s ← lift (Array.to_slice uvc_xfer_func.XFER_FUNCS)
  let i ← lift (UScalar.cast .Usize transfer_characteristics)
  let o ←
    core.slice.Slice.get (core.slice.index.SliceIndexUsizeSlice Std.I32) s i
  match o with
  | none => ok V4L2_XFER_FUNC_DEFAULT
  | some c => ok c

/-- [uvc_verify::uvc_ycbcr_enc::YCBCR_ENCS]
    Source: 'src/lib.rs', lines 303:4-310:6 -/
@[global_simps, irreducible]
def uvc_ycbcr_enc.YCBCR_ENCS : Array Std.I32 6#usize :=
  Array.make 6#usize [
    V4L2_YCBCR_ENC_DEFAULT, V4L2_YCBCR_ENC_709, V4L2_YCBCR_ENC_601,
    V4L2_YCBCR_ENC_601, V4L2_YCBCR_ENC_601, V4L2_YCBCR_ENC_SMPTE240M
    ]

/-- [uvc_verify::uvc_ycbcr_enc]:
    Source: 'src/lib.rs', lines 302:0-315:1 -/
def uvc_ycbcr_enc (matrix_coefficients : Std.U8) : Result Std.I32 := do
  let s ← lift (Array.to_slice uvc_ycbcr_enc.YCBCR_ENCS)
  let i ← lift (UScalar.cast .Usize matrix_coefficients)
  let o ←
    core.slice.Slice.get (core.slice.index.SliceIndexUsizeSlice Std.I32) s i
  match o with
  | none => ok V4L2_YCBCR_ENC_DEFAULT
  | some c => ok c

/-- [uvc_verify::v4l2_format_info]:
    Source: 'src/lib.rs', lines 320:0-322:1 -/
def v4l2_format_info (_fcc : Std.U32) : Result (Option Unit) := do
  ok none

/-- [uvc_verify::UvcFormat]
    Source: 'src/lib.rs', lines 330:0-343:1 -/
structure UvcFormat where
  type_ : Std.U8
  index : Std.U8
  bpp : Std.U8
  colorspace : Std.I32
  xfer_func : Std.I32
  ycbcr_enc : Std.I32
  fcc : Std.U32
  flags : Std.U32
  nframes : Std.U32
  frame_base : Std.Usize

/-- [uvc_verify::{impl core::default::Default for uvc_verify::UvcFormat}::default]:
    Source: 'src/lib.rs', lines 329:9-329:16
    Visibility: public -/
def UvcFormat.Insts.CoreDefaultDefault.default : Result UvcFormat := do
  ok
    {
      type_ := (core.default.DefaultU8.default),
      index := (core.default.DefaultU8.default),
      bpp := (core.default.DefaultU8.default),
      colorspace := (core.default.DefaultI32.default),
      xfer_func := (core.default.DefaultI32.default),
      ycbcr_enc := (core.default.DefaultI32.default),
      fcc := (core.default.DefaultU32.default),
      flags := (core.default.DefaultU32.default),
      nframes := (core.default.DefaultU32.default),
      frame_base := (core.default.DefaultUsize.default)
    }

/-- Trait implementation: [uvc_verify::{impl core::default::Default for uvc_verify::UvcFormat}]
    Source: 'src/lib.rs', lines 329:9-329:16 -/
@[reducible]
def UvcFormat.Insts.CoreDefaultDefault : core.default.Default UvcFormat := {
  default := UvcFormat.Insts.CoreDefaultDefault.default
}

/-- [uvc_verify::{impl core::clone::Clone for uvc_verify::UvcFormat}::clone]:
    Source: 'src/lib.rs', lines 329:18-329:23
    Visibility: public -/
def UvcFormat.Insts.CoreCloneClone.clone
  (self : UvcFormat) : Result UvcFormat := do
  let i ← lift (core.clone.impls.CloneU8.clone self.type_)
  let i1 ← lift (core.clone.impls.CloneU8.clone self.index)
  let i2 ← lift (core.clone.impls.CloneU8.clone self.bpp)
  let i3 ← lift (core.clone.impls.CloneI32.clone self.colorspace)
  let i4 ← lift (core.clone.impls.CloneI32.clone self.xfer_func)
  let i5 ← lift (core.clone.impls.CloneI32.clone self.ycbcr_enc)
  let i6 ← lift (core.clone.impls.CloneU32.clone self.fcc)
  let i7 ← lift (core.clone.impls.CloneU32.clone self.flags)
  let i8 ← lift (core.clone.impls.CloneU32.clone self.nframes)
  let i9 ← lift (core.clone.impls.CloneUsize.clone self.frame_base)
  ok
    {
      type_ := i,
      index := i1,
      bpp := i2,
      colorspace := i3,
      xfer_func := i4,
      ycbcr_enc := i5,
      fcc := i6,
      flags := i7,
      nframes := i8,
      frame_base := i9
    }

/-- Trait implementation: [uvc_verify::{impl core::clone::Clone for uvc_verify::UvcFormat}]
    Source: 'src/lib.rs', lines 329:18-329:23 -/
@[reducible]
def UvcFormat.Insts.CoreCloneClone : core.clone.Clone UvcFormat := {
  clone := UvcFormat.Insts.CoreCloneClone.clone
}

/-- [uvc_verify::UvcFrame]
    Source: 'src/lib.rs', lines 346:0-359:1 -/
structure UvcFrame where
  b_frame_index : Std.U8
  bm_capabilities : Std.U8
  w_width : Std.U16
  w_height : Std.U16
  dw_min_bit_rate : Std.U32
  dw_max_bit_rate : Std.U32
  dw_max_video_frame_buffer_size : Std.U32
  b_frame_interval_type : Std.U8
  dw_default_frame_interval : Std.U32
  interval_base : Std.I32

/-- [uvc_verify::{impl core::default::Default for uvc_verify::UvcFrame}::default]:
    Source: 'src/lib.rs', lines 345:9-345:16
    Visibility: public -/
def UvcFrame.Insts.CoreDefaultDefault.default : Result UvcFrame := do
  ok
    {
      b_frame_index := (core.default.DefaultU8.default),
      bm_capabilities := (core.default.DefaultU8.default),
      w_width := (core.default.DefaultU16.default),
      w_height := (core.default.DefaultU16.default),
      dw_min_bit_rate := (core.default.DefaultU32.default),
      dw_max_bit_rate := (core.default.DefaultU32.default),
      dw_max_video_frame_buffer_size := (core.default.DefaultU32.default),
      b_frame_interval_type := (core.default.DefaultU8.default),
      dw_default_frame_interval := (core.default.DefaultU32.default),
      interval_base := (core.default.DefaultI32.default)
    }

/-- Trait implementation: [uvc_verify::{impl core::default::Default for uvc_verify::UvcFrame}]
    Source: 'src/lib.rs', lines 345:9-345:16 -/
@[reducible]
def UvcFrame.Insts.CoreDefaultDefault : core.default.Default UvcFrame := {
  default := UvcFrame.Insts.CoreDefaultDefault.default
}

/-- [uvc_verify::{impl core::clone::Clone for uvc_verify::UvcFrame}::clone]:
    Source: 'src/lib.rs', lines 345:18-345:23
    Visibility: public -/
def UvcFrame.Insts.CoreCloneClone.clone
  (self : UvcFrame) : Result UvcFrame := do
  let i ← lift (core.clone.impls.CloneU8.clone self.b_frame_index)
  let i1 ← lift (core.clone.impls.CloneU8.clone self.bm_capabilities)
  let i2 ← lift (core.clone.impls.CloneU16.clone self.w_width)
  let i3 ← lift (core.clone.impls.CloneU16.clone self.w_height)
  let i4 ← lift (core.clone.impls.CloneU32.clone self.dw_min_bit_rate)
  let i5 ← lift (core.clone.impls.CloneU32.clone self.dw_max_bit_rate)
  let i6 ←
    lift (core.clone.impls.CloneU32.clone self.dw_max_video_frame_buffer_size)
  let i7 ← lift (core.clone.impls.CloneU8.clone self.b_frame_interval_type)
  let i8 ←
    lift (core.clone.impls.CloneU32.clone self.dw_default_frame_interval)
  let i9 ← lift (core.clone.impls.CloneI32.clone self.interval_base)
  ok
    {
      b_frame_index := i,
      bm_capabilities := i1,
      w_width := i2,
      w_height := i3,
      dw_min_bit_rate := i4,
      dw_max_bit_rate := i5,
      dw_max_video_frame_buffer_size := i6,
      b_frame_interval_type := i7,
      dw_default_frame_interval := i8,
      interval_base := i9
    }

/-- Trait implementation: [uvc_verify::{impl core::clone::Clone for uvc_verify::UvcFrame}]
    Source: 'src/lib.rs', lines 345:18-345:23 -/
@[reducible]
def UvcFrame.Insts.CoreCloneClone : core.clone.Clone UvcFrame := {
  clone := UvcFrame.Insts.CoreCloneClone.clone
}

/-- [uvc_verify::le16]:
    Source: 'src/lib.rs', lines 364:0-366:1 -/
def le16 (buf : Slice Std.U8) (at1 : Std.Usize) : Result Std.U16 := do
  let i ← Slice.index_usize buf at1
  let i1 ← lift (UScalar.cast .U16 i)
  let i2 ← at1 + 1#usize
  let i3 ← Slice.index_usize buf i2
  let i4 ← lift (UScalar.cast .U16 i3)
  let i5 ← i4 <<< 8#i32
  ok (i1 ||| i5)

/-- [uvc_verify::le32]:
    Source: 'src/lib.rs', lines 367:0-372:1 -/
def le32 (buf : Slice Std.U8) (at1 : Std.Usize) : Result Std.U32 := do
  let i ← Slice.index_usize buf at1
  let i1 ← lift (UScalar.cast .U32 i)
  let i2 ← at1 + 1#usize
  let i3 ← Slice.index_usize buf i2
  let i4 ← lift (UScalar.cast .U32 i3)
  let i5 ← i4 <<< 8#i32
  let i6 ← lift (i1 ||| i5)
  let i7 ← at1 + 2#usize
  let i8 ← Slice.index_usize buf i7
  let i9 ← lift (UScalar.cast .U32 i8)
  let i10 ← i9 <<< 16#i32
  let i11 ← lift (i6 ||| i10)
  let i12 ← at1 + 3#usize
  let i13 ← Slice.index_usize buf i12
  let i14 ← lift (UScalar.cast .U32 i13)
  let i15 ← i14 <<< 24#i32
  ok (i11 ||| i15)

/-- [uvc_verify::clamp_u32]:
    Source: 'src/lib.rs', lines 376:0-378:1 -/
def clamp_u32
  (val : Std.U32) (lo : Std.U32) (hi : Std.U32) : Result Std.U32 := do
  let i ← lift (core.cmp.impls.OrdU32.max val lo)
  ok (core.cmp.impls.OrdU32.min i hi)

/-- [uvc_verify::ParseState]
    Source: 'src/lib.rs', lines 380:0-384:1 -/
structure ParseState where
  frames : alloc.vec.Vec UvcFrame
  intervals : alloc.vec.Vec Std.U32
  interval_cursor : Std.Usize

/-- [uvc_verify::{uvc_verify::ParseState}::uvc_parse_frame]: loop body 0:
    Source: 'src/lib.rs', lines 447:8-452:9 -/
@[rust_loop_body]
def ParseState.uvc_parse_frame_loop.body
  (iv_base : Std.Usize) (buf : Slice Std.U8) (pos : Std.Usize)
  (iter : core.ops.range.Range Std.Usize) (v : alloc.vec.Vec Std.U32) :
  Result (ControlFlow ((core.ops.range.Range Std.Usize) × (alloc.vec.Vec
    Std.U32)) (alloc.vec.Vec Std.U32))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done v)
  | some i =>
    let i1 ← pos + 26#usize
    let i2 ← 4#usize * i
    let i3 ← i1 + i2
    let interval ← le32 buf i3
    let interval1 ← if interval != 0#u32
                      then ok interval
                      else ok 1#u32
    let i4 ← iv_base + i
    let (_, index_mut_back) ←
      alloc.vec.Vec.index_mut (core.slice.index.SliceIndexUsizeSlice Std.U32) v
        i4
    let v1 := index_mut_back interval1
    ok (cont (iter1, v1))

/-- [uvc_verify::{uvc_verify::ParseState}::uvc_parse_frame]: loop 0:
    Source: 'src/lib.rs', lines 447:8-452:9 -/
@[rust_loop]
def ParseState.uvc_parse_frame_loop
  (iter : core.ops.range.Range Std.Usize) (v : alloc.vec.Vec Std.U32)
  (iv_base : Std.Usize) (buf : Slice Std.U8) (pos : Std.Usize) :
  Result (alloc.vec.Vec Std.U32)
  := do
  loop
    (fun (iter1, v1) => ParseState.uvc_parse_frame_loop.body iv_base buf pos
      iter1 v1)
    (iter, v)

/-- [uvc_verify::{uvc_verify::ParseState}::uvc_parse_frame]:
    Source: 'src/lib.rs', lines 392:4-503:5 -/
def ParseState.uvc_parse_frame
  (self : ParseState) (dev_quirks : Std.U32) (format : UvcFormat)
  (frame_idx : Std.Usize) (ftype : Std.U8) (width_multiplier : Std.I32)
  (buf : Slice Std.U8) (pos : Std.Usize) (buflen : Std.I32) :
  Result (Std.I32 × ParseState)
  := do
  let n ←
    if ftype != UVC_VS_FRAME_FRAME_BASED
    then
      if buflen > 25#i32
      then
        do
        let i ← pos + 25#usize
        let i1 ← Slice.index_usize buf i
        ok (UScalar.cast .U32 i1)
      else ok 0#u32
    else
      if buflen > 21#i32
      then
        do
        let i ← pos + 21#usize
        let i1 ← Slice.index_usize buf i
        ok (UScalar.cast .U32 i1)
      else ok 0#u32
  let n1 ← if n != 0#u32
             then ok n
             else ok 3#u32
  let i ← lift (IScalar.hcast .U32 buflen)
  let i1 ← 4#u32 * n1
  let i2 ← 26#u32 + i1
  if i < i2
  then let i3 ← -. EINVAL
       ok (i3, self)
  else
    let _ ← UvcFrame.Insts.CoreDefaultDefault.default
    let i3 ← pos + 3#usize
    let i4 ← Slice.index_usize buf i3
    let i5 ← pos + 4#usize
    let i6 ← Slice.index_usize buf i5
    let i7 ← pos + 5#usize
    let i8 ← le16 buf i7
    let i9 ← lift (UScalar.hcast .I32 i8)
    let i10 ← lift (core.num.I32.wrapping_mul i9 width_multiplier)
    let i11 ← lift (IScalar.hcast .U16 i10)
    let i12 ← pos + 7#usize
    let i13 ← le16 buf i12
    let i14 ← pos + 9#usize
    let i15 ← le32 buf i14
    let i16 ← pos + 13#usize
    let i17 ← le32 buf i16
    let (i18, i19, i20) ←
      if ftype != UVC_VS_FRAME_FRAME_BASED
      then
        do
        let i21 ← pos + 17#usize
        let i22 ← le32 buf i21
        let i23 ← pos + 21#usize
        let i24 ← le32 buf i23
        let i25 ← pos + 25#usize
        let i26 ← Slice.index_usize buf i25
        ok (i22, i26, i24)
      else
        do
        let i21 ← pos + 17#usize
        let i22 ← le32 buf i21
        let i23 ← pos + 21#usize
        let i24 ← Slice.index_usize buf i23
        ok (0#u32, i24, i22)
    let i21 ← lift (UScalar.hcast .I32 self.interval_cursor)
    let i22 ← lift (UScalar.cast .Usize n1)
    let v ←
      ParseState.uvc_parse_frame_loop { start := 0#usize, «end» := i22 }
        self.intervals self.interval_cursor buf pos
    let i23 ← lift (format.flags &&& UVC_FMT_FLAG_COMPRESSED)
    let i24 ←
      if i23 = 0#u32
      then
        do
        let i25 ← lift (UScalar.cast .U32 format.bpp)
        let i26 ← lift (UScalar.cast .U32 i11)
        let i27 ← lift (core.num.U32.wrapping_mul i25 i26)
        let i28 ← lift (UScalar.cast .U32 i13)
        let prod ← lift (core.num.U32.wrapping_mul i27 i28)
        prod >>> 3#i32
      else ok i18
    let max_interval_index ←
      if i19 != 0#u8
      then do
           let i25 ← n1 - 1#u32
           ok (UScalar.cast .Usize i25)
      else ok 1#usize
    let i25 ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.U32) v
        self.interval_cursor
    let i26 ← self.interval_cursor + max_interval_index
    let i27 ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.U32) v i26
    let i28 ← clamp_u32 i20 i25 i27
    let i29 ← lift (dev_quirks &&& UVC_QUIRK_RESTRICT_FRAME_RATE)
    let (v1, i30) ←
      if i29 != 0#u32
      then
        do
        let (_, index_mut_back) ←
          alloc.vec.Vec.index_mut (core.slice.index.SliceIndexUsizeSlice
            Std.U32) v self.interval_cursor
        let v2 := index_mut_back i28
        ok (v2, 1#u8)
      else ok (v, i19)
    let i31 ← lift (UScalar.cast .Usize n1)
    let i32 ← self.interval_cursor + i31
    let (_, index_mut_back) ←
      alloc.vec.Vec.index_mut (core.slice.index.SliceIndexUsizeSlice UvcFrame)
        self.frames frame_idx
    let i33 ← Slice.index_usize buf pos
    let i34 ← lift (UScalar.hcast .I32 i33)
    let v2 :=
      index_mut_back
        {
          b_frame_index := i4,
          bm_capabilities := i6,
          w_width := i11,
          w_height := i13,
          dw_min_bit_rate := i15,
          dw_max_bit_rate := i17,
          dw_max_video_frame_buffer_size := i24,
          b_frame_interval_type := i30,
          dw_default_frame_interval := i28,
          interval_base := i21
        }
    ok (i34, { frames := v2, intervals := v1, interval_cursor := i32 })

/-- [uvc_verify::{uvc_verify::ParseState}::uvc_parse_format]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-652:13 -/
@[rust_loop_body]
def ParseState.uvc_parse_format_loop0.body
  (dev_quirks : Std.U32) (i : Std.U8) (i1 : Std.U8) (i2 : Std.U8)
  (i3 : Std.I32) (i4 : Std.I32) (i5 : Std.I32) (i6 : Std.U32) (i7 : Std.U32)
  (frame_base : Std.Usize) (buf : Slice Std.U8) (width_multiplier : Std.I32)
  (ftype : Std.U8) (self : ParseState) (i8 : Std.U32) (pos : Std.Usize)
  (buflen : Std.I32) :
  Result (ControlFlow (ParseState × Std.U32 × Std.Usize × Std.I32)
    (ParseState × Std.U32 × Std.Usize × Std.I32 × Std.I32))
  := do
  if buflen > 2#i32
  then
    let i9 ← pos + 1#usize
    let i10 ← Slice.index_usize buf i9
    if i10 = USB_DT_CS_INTERFACE
    then
      let i11 ← pos + 2#usize
      let i12 ← Slice.index_usize buf i11
      if i12 = ftype
      then
        let i13 ← lift (UScalar.cast .Usize i8)
        let frame_idx ← frame_base + i13
        let (ret, self1) ←
          ParseState.uvc_parse_frame self dev_quirks
            {
              type_ := i,
              index := i1,
              bpp := i2,
              colorspace := i3,
              xfer_func := i4,
              ycbcr_enc := i5,
              fcc := i6,
              flags := i7,
              nframes := i8,
              frame_base
            } frame_idx ftype width_multiplier buf pos buflen
        if ret < 0#i32
        then ok (done (self1, i8, pos, buflen, ret))
        else
          let i14 ← i8 + 1#u32
          let buflen1 ← buflen - ret
          let i15 ← lift (IScalar.hcast .Usize ret)
          let pos1 ← pos + i15
          ok (cont (self1, i14, pos1, buflen1))
      else ok (done (self, i8, pos, buflen, 0#i32))
    else ok (done (self, i8, pos, buflen, 0#i32))
  else ok (done (self, i8, pos, buflen, 0#i32))

/-- [uvc_verify::{uvc_verify::ParseState}::uvc_parse_format]: loop 0:
    Source: 'src/lib.rs', lines 1:0-652:13 -/
@[rust_loop]
def ParseState.uvc_parse_format_loop0
  (self : ParseState) (dev_quirks : Std.U32) (i : Std.U8) (i1 : Std.U8)
  (i2 : Std.U8) (i3 : Std.I32) (i4 : Std.I32) (i5 : Std.I32) (i6 : Std.U32)
  (i7 : Std.U32) (i8 : Std.U32) (frame_base : Std.Usize) (buf : Slice Std.U8)
  (pos : Std.Usize) (buflen : Std.I32) (width_multiplier : Std.I32)
  (ftype : Std.U8) :
  Result (ParseState × Std.U32 × Std.Usize × Std.I32 × Std.I32)
  := do
  loop
    (fun (self1, i9, pos1, buflen1) => ParseState.uvc_parse_format_loop0.body
      dev_quirks i i1 i2 i3 i4 i5 i6 i7 frame_base buf width_multiplier ftype
      self1 i9 pos1 buflen1)
    (self, i8, pos, buflen)

/-- [uvc_verify::{uvc_verify::ParseState}::uvc_parse_format]: loop body 1:
    Source: 'src/lib.rs', lines 1:0-652:13 -/
@[rust_loop_body]
def ParseState.uvc_parse_format_loop1.body
  (dev_quirks : Std.U32) (i : Std.U8) (i1 : Std.U8) (i2 : Std.U8)
  (i3 : Std.I32) (i4 : Std.I32) (i5 : Std.I32) (i6 : Std.U32) (i7 : Std.U32)
  (frame_base : Std.Usize) (buf : Slice Std.U8) (width_multiplier : Std.I32)
  (ftype : Std.U8) (self : ParseState) (i8 : Std.U32) (pos : Std.Usize)
  (buflen : Std.I32) :
  Result (ControlFlow (ParseState × Std.U32 × Std.Usize × Std.I32)
    (ParseState × Std.U32 × Std.Usize × Std.I32 × Std.I32))
  := do
  if buflen > 2#i32
  then
    let i9 ← pos + 1#usize
    let i10 ← Slice.index_usize buf i9
    if i10 = USB_DT_CS_INTERFACE
    then
      let i11 ← pos + 2#usize
      let i12 ← Slice.index_usize buf i11
      if i12 = ftype
      then
        let i13 ← lift (UScalar.cast .Usize i8)
        let frame_idx ← frame_base + i13
        let (ret, self1) ←
          ParseState.uvc_parse_frame self dev_quirks
            {
              type_ := i,
              index := i1,
              bpp := i2,
              colorspace := i3,
              xfer_func := i4,
              ycbcr_enc := i5,
              fcc := i6,
              flags := i7,
              nframes := i8,
              frame_base
            } frame_idx ftype width_multiplier buf pos buflen
        if ret < 0#i32
        then ok (done (self1, i8, pos, buflen, ret))
        else
          let i14 ← i8 + 1#u32
          let buflen1 ← buflen - ret
          let i15 ← lift (IScalar.hcast .Usize ret)
          let pos1 ← pos + i15
          ok (cont (self1, i14, pos1, buflen1))
      else ok (done (self, i8, pos, buflen, 0#i32))
    else ok (done (self, i8, pos, buflen, 0#i32))
  else ok (done (self, i8, pos, buflen, 0#i32))

/-- [uvc_verify::{uvc_verify::ParseState}::uvc_parse_format]: loop 1:
    Source: 'src/lib.rs', lines 1:0-652:13 -/
@[rust_loop]
def ParseState.uvc_parse_format_loop1
  (self : ParseState) (dev_quirks : Std.U32) (i : Std.U8) (i1 : Std.U8)
  (i2 : Std.U8) (i3 : Std.I32) (i4 : Std.I32) (i5 : Std.I32) (i6 : Std.U32)
  (i7 : Std.U32) (i8 : Std.U32) (frame_base : Std.Usize) (buf : Slice Std.U8)
  (pos : Std.Usize) (buflen : Std.I32) (width_multiplier : Std.I32)
  (ftype : Std.U8) :
  Result (ParseState × Std.U32 × Std.Usize × Std.I32 × Std.I32)
  := do
  loop
    (fun (self1, i9, pos1, buflen1) => ParseState.uvc_parse_format_loop1.body
      dev_quirks i i1 i2 i3 i4 i5 i6 i7 frame_base buf width_multiplier ftype
      self1 i9 pos1 buflen1)
    (self, i8, pos, buflen)

/-- [uvc_verify::{uvc_verify::ParseState}::uvc_parse_format]: loop body 2:
    Source: 'src/lib.rs', lines 1:0-652:13 -/
@[rust_loop_body]
def ParseState.uvc_parse_format_loop2.body
  (dev_quirks : Std.U32) (i : Std.U8) (i1 : Std.U8) (i2 : Std.I32)
  (i3 : Std.I32) (i4 : Std.I32) (i5 : Std.U32) (i6 : Std.U32)
  (frame_base : Std.Usize) (buf : Slice Std.U8) (ftype : Std.U8)
  (self : ParseState) (i7 : Std.U32) (pos : Std.Usize) (buflen : Std.I32) :
  Result (ControlFlow (ParseState × Std.U32 × Std.Usize × Std.I32)
    (ParseState × Std.U32 × Std.Usize × Std.I32 × Std.I32))
  := do
  if buflen > 2#i32
  then
    let i8 ← pos + 1#usize
    let i9 ← Slice.index_usize buf i8
    if i9 = USB_DT_CS_INTERFACE
    then
      let i10 ← pos + 2#usize
      let i11 ← Slice.index_usize buf i10
      if i11 = ftype
      then
        let i12 ← lift (UScalar.cast .Usize i7)
        let frame_idx ← frame_base + i12
        let (ret, self1) ←
          ParseState.uvc_parse_frame self dev_quirks
            {
              type_ := i,
              index := i1,
              bpp := 0#u8,
              colorspace := i2,
              xfer_func := i3,
              ycbcr_enc := i4,
              fcc := i5,
              flags := i6,
              nframes := i7,
              frame_base
            } frame_idx ftype 1#i32 buf pos buflen
        if ret < 0#i32
        then ok (done (self1, i7, pos, buflen, ret))
        else
          let i13 ← i7 + 1#u32
          let buflen1 ← buflen - ret
          let i14 ← lift (IScalar.hcast .Usize ret)
          let pos1 ← pos + i14
          ok (cont (self1, i13, pos1, buflen1))
      else ok (done (self, i7, pos, buflen, 0#i32))
    else ok (done (self, i7, pos, buflen, 0#i32))
  else ok (done (self, i7, pos, buflen, 0#i32))

/-- [uvc_verify::{uvc_verify::ParseState}::uvc_parse_format]: loop 2:
    Source: 'src/lib.rs', lines 1:0-652:13 -/
@[rust_loop]
def ParseState.uvc_parse_format_loop2
  (self : ParseState) (dev_quirks : Std.U32) (i : Std.U8) (i1 : Std.U8)
  (i2 : Std.I32) (i3 : Std.I32) (i4 : Std.I32) (i5 : Std.U32) (i6 : Std.U32)
  (i7 : Std.U32) (frame_base : Std.Usize) (buf : Slice Std.U8)
  (pos : Std.Usize) (buflen : Std.I32) (ftype : Std.U8) :
  Result (ParseState × Std.U32 × Std.Usize × Std.I32 × Std.I32)
  := do
  loop
    (fun (self1, i8, pos1, buflen1) => ParseState.uvc_parse_format_loop2.body
      dev_quirks i i1 i2 i3 i4 i5 i6 frame_base buf ftype self1 i8 pos1
      buflen1)
    (self, i7, pos, buflen)

/-- [uvc_verify::{uvc_verify::ParseState}::uvc_parse_format]: loop body 3:
    Source: 'src/lib.rs', lines 1:0-652:13 -/
@[rust_loop_body]
def ParseState.uvc_parse_format_loop3.body
  (dev_quirks : Std.U32) (i : Std.U8) (i1 : Std.U8) (i2 : Std.I32)
  (i3 : Std.I32) (i4 : Std.I32) (i5 : Std.U32) (i6 : Std.U32)
  (frame_base : Std.Usize) (buf : Slice Std.U8) (self : ParseState)
  (i7 : Std.U32) (pos : Std.Usize) (buflen : Std.I32) :
  Result (ControlFlow (ParseState × Std.U32 × Std.Usize × Std.I32)
    (ParseState × Std.U32 × Std.Usize × Std.I32 × Std.I32))
  := do
  if buflen > 2#i32
  then
    let i8 ← pos + 1#usize
    let i9 ← Slice.index_usize buf i8
    if i9 = USB_DT_CS_INTERFACE
    then
      let i10 ← pos + 2#usize
      let i11 ← Slice.index_usize buf i10
      if i11 = 0#u8
      then
        let i12 ← lift (UScalar.cast .Usize i7)
        let frame_idx ← frame_base + i12
        let (ret, self1) ←
          ParseState.uvc_parse_frame self dev_quirks
            {
              type_ := i,
              index := i1,
              bpp := 0#u8,
              colorspace := i2,
              xfer_func := i3,
              ycbcr_enc := i4,
              fcc := i5,
              flags := i6,
              nframes := i7,
              frame_base
            } frame_idx 0#u8 1#i32 buf pos buflen
        if ret < 0#i32
        then ok (done (self1, i7, pos, buflen, ret))
        else
          let i13 ← i7 + 1#u32
          let buflen1 ← buflen - ret
          let i14 ← lift (IScalar.hcast .Usize ret)
          let pos1 ← pos + i14
          ok (cont (self1, i13, pos1, buflen1))
      else ok (done (self, i7, pos, buflen, 0#i32))
    else ok (done (self, i7, pos, buflen, 0#i32))
  else ok (done (self, i7, pos, buflen, 0#i32))

/-- [uvc_verify::{uvc_verify::ParseState}::uvc_parse_format]: loop 3:
    Source: 'src/lib.rs', lines 1:0-652:13 -/
@[rust_loop]
def ParseState.uvc_parse_format_loop3
  (self : ParseState) (dev_quirks : Std.U32) (i : Std.U8) (i1 : Std.U8)
  (i2 : Std.I32) (i3 : Std.I32) (i4 : Std.I32) (i5 : Std.U32) (i6 : Std.U32)
  (i7 : Std.U32) (frame_base : Std.Usize) (buf : Slice Std.U8)
  (pos : Std.Usize) (buflen : Std.I32) :
  Result (ParseState × Std.U32 × Std.Usize × Std.I32 × Std.I32)
  := do
  loop
    (fun (self1, i8, pos1, buflen1) => ParseState.uvc_parse_format_loop3.body
      dev_quirks i i1 i2 i3 i4 i5 i6 frame_base buf self1 i8 pos1 buflen1)
    (self, i7, pos, buflen)

/-- [uvc_verify::{uvc_verify::ParseState}::uvc_parse_format]:
    Source: 'src/lib.rs', lines 513:4-683:5 -/
def ParseState.uvc_parse_format
  (self : ParseState) (dev_quirks : Std.U32) (format : UvcFormat)
  (frame_base : Std.Usize) (buf : Slice Std.U8) (start_pos : Std.Usize)
  (start_buflen : Std.I32) :
  Result (Std.I32 × ParseState × UvcFormat)
  := do
  if start_buflen < 4#i32
  then let frame_err ← -. EINVAL
       ok (frame_err, self, format)
  else
    let i ← start_pos + 2#usize
    let i1 ← Slice.index_usize buf i
    let i2 ← start_pos + 3#usize
    let i3 ← Slice.index_usize buf i2
    let i4 ← Slice.index_usize buf i
    match i4 with
    | 4#uscalar =>
      let i5 ← Slice.index_usize buf i
      let n ←
        if i5 = UVC_VS_FORMAT_UNCOMPRESSED
        then ok 27#u32
        else ok 28#u32
      let i6 ← lift (IScalar.hcast .U32 start_buflen)
      if i6 < n
      then
        let frame_err ← -. EINVAL
        ok (frame_err, self,
          { format with type_ := i1, index := i3, frame_base })
      else
        let i7 ← start_pos + 5#usize
        let s ←
          core.slice.index.Slice.index
            (core.slice.index.SliceIndexRangeFromUsizeSlice Std.U8) buf
            { start := i7 }
        let o ← uvc_format_by_guid s
        match o with
        | none =>
          ok (0#i32, self,
            { format with type_ := i1, index := i3, frame_base })
        | some fcc =>
          let i8 ← start_pos + 21#usize
          let i9 ← Slice.index_usize buf i8
          let i10 ← lift (dev_quirks &&& UVC_QUIRK_FORCE_Y8)
          let (i11, i12, width_multiplier) ←
            if i10 != 0#u32
            then
              do
              let i13 ← V4L2_PIX_FMT_YUYV
              if fcc = i13
              then let i14 ← V4L2_PIX_FMT_GREY
                   ok (8#u8, i14, 2#i32)
              else ok (i9, fcc, 1#i32)
            else ok (i9, fcc, 1#i32)
          let i13 ← lift (dev_quirks &&& UVC_QUIRK_FORCE_BPP)
          if i13 != 0#u32
          then
            do
            let o1 ← v4l2_format_info i12
            match o1 with
            | none => ok ()
            | some _ => fail panic
          else ok ()
          let i14 ← Slice.index_usize buf i
          let (i15, ftype) ←
            if i14 = UVC_VS_FORMAT_UNCOMPRESSED
            then ok (format.flags, UVC_VS_FRAME_UNCOMPRESSED)
            else
              do
              let i16 ← start_pos + 27#usize
              let i17 ← Slice.index_usize buf i16
              let i18 ←
                if i17 != 0#u8
                then ok UVC_FMT_FLAG_COMPRESSED
                else ok format.flags
              ok (i18, UVC_VS_FRAME_FRAME_BASED)
          let i16 ← Slice.index_usize buf start_pos
          let i17 ← lift (UScalar.hcast .I32 i16)
          let buflen ← start_buflen - i17
          let i18 ← lift (UScalar.cast .Usize i16)
          let pos ← start_pos + i18
          if ftype != 0#u8
          then
            let (self1, i19, pos1, buflen1, frame_err) ←
              ParseState.uvc_parse_format_loop0 self dev_quirks i1 i3 i11
                format.colorspace format.xfer_func format.ycbcr_enc i12 i15
                format.nframes frame_base buf pos buflen width_multiplier ftype
            if frame_err < 0#i32
            then
              ok (frame_err, self1,
                {
                  format
                    with
                    type_ := i1,
                    index := i3,
                    bpp := i11,
                    fcc := i12,
                    flags := i15,
                    nframes := i19,
                    frame_base
                })
            else
              let (pos2, buflen2) ←
                if buflen1 > 2#i32
                then
                  do
                  let i20 ← pos1 + 1#usize
                  let i21 ← Slice.index_usize buf i20
                  if i21 = USB_DT_CS_INTERFACE
                  then
                    let i22 ← pos1 + 2#usize
                    let i23 ← Slice.index_usize buf i22
                    if i23 = UVC_VS_STILL_IMAGE_FRAME
                    then
                      let i24 ← Slice.index_usize buf pos1
                      let i25 ← lift (UScalar.hcast .I32 i24)
                      let buflen3 ← buflen1 - i25
                      let i26 ← lift (UScalar.cast .Usize i24)
                      let pos3 ← pos1 + i26
                      ok (pos3, buflen3)
                    else ok (pos1, buflen1)
                  else ok (pos1, buflen1)
                else ok (pos1, buflen1)
              if buflen2 > 2#i32
              then
                let i20 ← pos2 + 1#usize
                let i21 ← Slice.index_usize buf i20
                if i21 = USB_DT_CS_INTERFACE
                then
                  let i22 ← pos2 + 2#usize
                  let i23 ← Slice.index_usize buf i22
                  if i23 = UVC_VS_COLORFORMAT
                  then
                    if buflen2 < 6#i32
                    then
                      let frame_err1 ← -. EINVAL
                      ok (frame_err1, self1,
                        {
                          format
                            with
                            type_ := i1,
                            index := i3,
                            bpp := i11,
                            fcc := i12,
                            flags := i15,
                            nframes := i19,
                            frame_base
                        })
                    else
                      let i24 ← pos2 + 3#usize
                      let i25 ← Slice.index_usize buf i24
                      let i26 ← uvc_colorspace i25
                      let i27 ← pos2 + 4#usize
                      let i28 ← Slice.index_usize buf i27
                      let i29 ← uvc_xfer_func i28
                      let i30 ← pos2 + 5#usize
                      let i31 ← Slice.index_usize buf i30
                      let i32 ← uvc_ycbcr_enc i31
                      let i33 ← Slice.index_usize buf pos2
                      let i34 ← lift (UScalar.hcast .I32 i33)
                      let _ ← buflen2 - i34
                      let i35 ← lift (UScalar.cast .Usize i33)
                      let pos3 ← pos2 + i35
                      let i36 ← pos3 - start_pos
                      let frame_err1 ← lift (UScalar.hcast .I32 i36)
                      ok (frame_err1, self1,
                        {
                          type_ := i1,
                          index := i3,
                          bpp := i11,
                          colorspace := i26,
                          xfer_func := i29,
                          ycbcr_enc := i32,
                          fcc := i12,
                          flags := i15,
                          nframes := i19,
                          frame_base
                        })
                  else
                    let i24 ← pos2 - start_pos
                    let frame_err1 ← lift (UScalar.hcast .I32 i24)
                    ok (frame_err1, self1,
                      {
                        format
                          with
                          type_ := i1,
                          index := i3,
                          bpp := i11,
                          colorspace := V4L2_COLORSPACE_SRGB,
                          fcc := i12,
                          flags := i15,
                          nframes := i19,
                          frame_base
                      })
                else
                  let i22 ← pos2 - start_pos
                  let frame_err1 ← lift (UScalar.hcast .I32 i22)
                  ok (frame_err1, self1,
                    {
                      format
                        with
                        type_ := i1,
                        index := i3,
                        bpp := i11,
                        colorspace := V4L2_COLORSPACE_SRGB,
                        fcc := i12,
                        flags := i15,
                        nframes := i19,
                        frame_base
                    })
              else
                let i20 ← pos2 - start_pos
                let frame_err1 ← lift (UScalar.hcast .I32 i20)
                ok (frame_err1, self1,
                  {
                    format
                      with
                      type_ := i1,
                      index := i3,
                      bpp := i11,
                      colorspace := V4L2_COLORSPACE_SRGB,
                      fcc := i12,
                      flags := i15,
                      nframes := i19,
                      frame_base
                  })
          else
            let (pos1, buflen1) ←
              if buflen > 2#i32
              then
                do
                let i19 ← pos + 1#usize
                let i20 ← Slice.index_usize buf i19
                if i20 = USB_DT_CS_INTERFACE
                then
                  let i21 ← pos + 2#usize
                  let i22 ← Slice.index_usize buf i21
                  if i22 = UVC_VS_STILL_IMAGE_FRAME
                  then
                    let i23 ← Slice.index_usize buf pos
                    let i24 ← lift (UScalar.hcast .I32 i23)
                    let buflen2 ← buflen - i24
                    let i25 ← lift (UScalar.cast .Usize i23)
                    let pos2 ← pos + i25
                    ok (pos2, buflen2)
                  else ok (pos, buflen)
                else ok (pos, buflen)
              else ok (pos, buflen)
            if buflen1 > 2#i32
            then
              let i19 ← pos1 + 1#usize
              let i20 ← Slice.index_usize buf i19
              if i20 = USB_DT_CS_INTERFACE
              then
                let i21 ← pos1 + 2#usize
                let i22 ← Slice.index_usize buf i21
                if i22 = UVC_VS_COLORFORMAT
                then
                  if buflen1 < 6#i32
                  then
                    let frame_err ← -. EINVAL
                    ok (frame_err, self,
                      {
                        format
                          with
                          type_ := i1,
                          index := i3,
                          bpp := i11,
                          fcc := i12,
                          flags := i15,
                          frame_base
                      })
                  else
                    let i23 ← pos1 + 3#usize
                    let i24 ← Slice.index_usize buf i23
                    let i25 ← uvc_colorspace i24
                    let i26 ← pos1 + 4#usize
                    let i27 ← Slice.index_usize buf i26
                    let i28 ← uvc_xfer_func i27
                    let i29 ← pos1 + 5#usize
                    let i30 ← Slice.index_usize buf i29
                    let i31 ← uvc_ycbcr_enc i30
                    let i32 ← Slice.index_usize buf pos1
                    let i33 ← lift (UScalar.hcast .I32 i32)
                    let _ ← buflen1 - i33
                    let i34 ← lift (UScalar.cast .Usize i32)
                    let pos2 ← pos1 + i34
                    let i35 ← pos2 - start_pos
                    let frame_err ← lift (UScalar.hcast .I32 i35)
                    ok (frame_err, self,
                      {
                        format
                          with
                          type_ := i1,
                          index := i3,
                          bpp := i11,
                          colorspace := i25,
                          xfer_func := i28,
                          ycbcr_enc := i31,
                          fcc := i12,
                          flags := i15,
                          frame_base
                      })
                else
                  let i23 ← pos1 - start_pos
                  let frame_err ← lift (UScalar.hcast .I32 i23)
                  ok (frame_err, self,
                    {
                      format
                        with
                        type_ := i1,
                        index := i3,
                        bpp := i11,
                        colorspace := V4L2_COLORSPACE_SRGB,
                        fcc := i12,
                        flags := i15,
                        frame_base
                    })
              else
                let i21 ← pos1 - start_pos
                let frame_err ← lift (UScalar.hcast .I32 i21)
                ok (frame_err, self,
                  {
                    format
                      with
                      type_ := i1,
                      index := i3,
                      bpp := i11,
                      colorspace := V4L2_COLORSPACE_SRGB,
                      fcc := i12,
                      flags := i15,
                      frame_base
                  })
            else
              let i19 ← pos1 - start_pos
              let frame_err ← lift (UScalar.hcast .I32 i19)
              ok (frame_err, self,
                {
                  format
                    with
                    type_ := i1,
                    index := i3,
                    bpp := i11,
                    colorspace := V4L2_COLORSPACE_SRGB,
                    fcc := i12,
                    flags := i15,
                    frame_base
                })
    | 16#uscalar =>
      let i5 ← Slice.index_usize buf i
      let n ←
        if i5 = UVC_VS_FORMAT_UNCOMPRESSED
        then ok 27#u32
        else ok 28#u32
      let i6 ← lift (IScalar.hcast .U32 start_buflen)
      if i6 < n
      then
        let frame_err ← -. EINVAL
        ok (frame_err, self,
          { format with type_ := i1, index := i3, frame_base })
      else
        let i7 ← start_pos + 5#usize
        let s ←
          core.slice.index.Slice.index
            (core.slice.index.SliceIndexRangeFromUsizeSlice Std.U8) buf
            { start := i7 }
        let o ← uvc_format_by_guid s
        match o with
        | none =>
          ok (0#i32, self,
            { format with type_ := i1, index := i3, frame_base })
        | some fcc =>
          let i8 ← start_pos + 21#usize
          let i9 ← Slice.index_usize buf i8
          let i10 ← lift (dev_quirks &&& UVC_QUIRK_FORCE_Y8)
          let (i11, i12, width_multiplier) ←
            if i10 != 0#u32
            then
              do
              let i13 ← V4L2_PIX_FMT_YUYV
              if fcc = i13
              then let i14 ← V4L2_PIX_FMT_GREY
                   ok (8#u8, i14, 2#i32)
              else ok (i9, fcc, 1#i32)
            else ok (i9, fcc, 1#i32)
          let i13 ← lift (dev_quirks &&& UVC_QUIRK_FORCE_BPP)
          if i13 != 0#u32
          then
            do
            let o1 ← v4l2_format_info i12
            match o1 with
            | none => ok ()
            | some _ => fail panic
          else ok ()
          let i14 ← Slice.index_usize buf i
          let (i15, ftype) ←
            if i14 = UVC_VS_FORMAT_UNCOMPRESSED
            then ok (format.flags, UVC_VS_FRAME_UNCOMPRESSED)
            else
              do
              let i16 ← start_pos + 27#usize
              let i17 ← Slice.index_usize buf i16
              let i18 ←
                if i17 != 0#u8
                then ok UVC_FMT_FLAG_COMPRESSED
                else ok format.flags
              ok (i18, UVC_VS_FRAME_FRAME_BASED)
          let i16 ← Slice.index_usize buf start_pos
          let i17 ← lift (UScalar.hcast .I32 i16)
          let buflen ← start_buflen - i17
          let i18 ← lift (UScalar.cast .Usize i16)
          let pos ← start_pos + i18
          if ftype != 0#u8
          then
            let (self1, i19, pos1, buflen1, frame_err) ←
              ParseState.uvc_parse_format_loop1 self dev_quirks i1 i3 i11
                format.colorspace format.xfer_func format.ycbcr_enc i12 i15
                format.nframes frame_base buf pos buflen width_multiplier ftype
            if frame_err < 0#i32
            then
              ok (frame_err, self1,
                {
                  format
                    with
                    type_ := i1,
                    index := i3,
                    bpp := i11,
                    fcc := i12,
                    flags := i15,
                    nframes := i19,
                    frame_base
                })
            else
              let (pos2, buflen2) ←
                if buflen1 > 2#i32
                then
                  do
                  let i20 ← pos1 + 1#usize
                  let i21 ← Slice.index_usize buf i20
                  if i21 = USB_DT_CS_INTERFACE
                  then
                    let i22 ← pos1 + 2#usize
                    let i23 ← Slice.index_usize buf i22
                    if i23 = UVC_VS_STILL_IMAGE_FRAME
                    then
                      let i24 ← Slice.index_usize buf pos1
                      let i25 ← lift (UScalar.hcast .I32 i24)
                      let buflen3 ← buflen1 - i25
                      let i26 ← lift (UScalar.cast .Usize i24)
                      let pos3 ← pos1 + i26
                      ok (pos3, buflen3)
                    else ok (pos1, buflen1)
                  else ok (pos1, buflen1)
                else ok (pos1, buflen1)
              if buflen2 > 2#i32
              then
                let i20 ← pos2 + 1#usize
                let i21 ← Slice.index_usize buf i20
                if i21 = USB_DT_CS_INTERFACE
                then
                  let i22 ← pos2 + 2#usize
                  let i23 ← Slice.index_usize buf i22
                  if i23 = UVC_VS_COLORFORMAT
                  then
                    if buflen2 < 6#i32
                    then
                      let frame_err1 ← -. EINVAL
                      ok (frame_err1, self1,
                        {
                          format
                            with
                            type_ := i1,
                            index := i3,
                            bpp := i11,
                            fcc := i12,
                            flags := i15,
                            nframes := i19,
                            frame_base
                        })
                    else
                      let i24 ← pos2 + 3#usize
                      let i25 ← Slice.index_usize buf i24
                      let i26 ← uvc_colorspace i25
                      let i27 ← pos2 + 4#usize
                      let i28 ← Slice.index_usize buf i27
                      let i29 ← uvc_xfer_func i28
                      let i30 ← pos2 + 5#usize
                      let i31 ← Slice.index_usize buf i30
                      let i32 ← uvc_ycbcr_enc i31
                      let i33 ← Slice.index_usize buf pos2
                      let i34 ← lift (UScalar.hcast .I32 i33)
                      let _ ← buflen2 - i34
                      let i35 ← lift (UScalar.cast .Usize i33)
                      let pos3 ← pos2 + i35
                      let i36 ← pos3 - start_pos
                      let frame_err1 ← lift (UScalar.hcast .I32 i36)
                      ok (frame_err1, self1,
                        {
                          type_ := i1,
                          index := i3,
                          bpp := i11,
                          colorspace := i26,
                          xfer_func := i29,
                          ycbcr_enc := i32,
                          fcc := i12,
                          flags := i15,
                          nframes := i19,
                          frame_base
                        })
                  else
                    let i24 ← pos2 - start_pos
                    let frame_err1 ← lift (UScalar.hcast .I32 i24)
                    ok (frame_err1, self1,
                      {
                        format
                          with
                          type_ := i1,
                          index := i3,
                          bpp := i11,
                          colorspace := V4L2_COLORSPACE_SRGB,
                          fcc := i12,
                          flags := i15,
                          nframes := i19,
                          frame_base
                      })
                else
                  let i22 ← pos2 - start_pos
                  let frame_err1 ← lift (UScalar.hcast .I32 i22)
                  ok (frame_err1, self1,
                    {
                      format
                        with
                        type_ := i1,
                        index := i3,
                        bpp := i11,
                        colorspace := V4L2_COLORSPACE_SRGB,
                        fcc := i12,
                        flags := i15,
                        nframes := i19,
                        frame_base
                    })
              else
                let i20 ← pos2 - start_pos
                let frame_err1 ← lift (UScalar.hcast .I32 i20)
                ok (frame_err1, self1,
                  {
                    format
                      with
                      type_ := i1,
                      index := i3,
                      bpp := i11,
                      colorspace := V4L2_COLORSPACE_SRGB,
                      fcc := i12,
                      flags := i15,
                      nframes := i19,
                      frame_base
                  })
          else
            let (pos1, buflen1) ←
              if buflen > 2#i32
              then
                do
                let i19 ← pos + 1#usize
                let i20 ← Slice.index_usize buf i19
                if i20 = USB_DT_CS_INTERFACE
                then
                  let i21 ← pos + 2#usize
                  let i22 ← Slice.index_usize buf i21
                  if i22 = UVC_VS_STILL_IMAGE_FRAME
                  then
                    let i23 ← Slice.index_usize buf pos
                    let i24 ← lift (UScalar.hcast .I32 i23)
                    let buflen2 ← buflen - i24
                    let i25 ← lift (UScalar.cast .Usize i23)
                    let pos2 ← pos + i25
                    ok (pos2, buflen2)
                  else ok (pos, buflen)
                else ok (pos, buflen)
              else ok (pos, buflen)
            if buflen1 > 2#i32
            then
              let i19 ← pos1 + 1#usize
              let i20 ← Slice.index_usize buf i19
              if i20 = USB_DT_CS_INTERFACE
              then
                let i21 ← pos1 + 2#usize
                let i22 ← Slice.index_usize buf i21
                if i22 = UVC_VS_COLORFORMAT
                then
                  if buflen1 < 6#i32
                  then
                    let frame_err ← -. EINVAL
                    ok (frame_err, self,
                      {
                        format
                          with
                          type_ := i1,
                          index := i3,
                          bpp := i11,
                          fcc := i12,
                          flags := i15,
                          frame_base
                      })
                  else
                    let i23 ← pos1 + 3#usize
                    let i24 ← Slice.index_usize buf i23
                    let i25 ← uvc_colorspace i24
                    let i26 ← pos1 + 4#usize
                    let i27 ← Slice.index_usize buf i26
                    let i28 ← uvc_xfer_func i27
                    let i29 ← pos1 + 5#usize
                    let i30 ← Slice.index_usize buf i29
                    let i31 ← uvc_ycbcr_enc i30
                    let i32 ← Slice.index_usize buf pos1
                    let i33 ← lift (UScalar.hcast .I32 i32)
                    let _ ← buflen1 - i33
                    let i34 ← lift (UScalar.cast .Usize i32)
                    let pos2 ← pos1 + i34
                    let i35 ← pos2 - start_pos
                    let frame_err ← lift (UScalar.hcast .I32 i35)
                    ok (frame_err, self,
                      {
                        format
                          with
                          type_ := i1,
                          index := i3,
                          bpp := i11,
                          colorspace := i25,
                          xfer_func := i28,
                          ycbcr_enc := i31,
                          fcc := i12,
                          flags := i15,
                          frame_base
                      })
                else
                  let i23 ← pos1 - start_pos
                  let frame_err ← lift (UScalar.hcast .I32 i23)
                  ok (frame_err, self,
                    {
                      format
                        with
                        type_ := i1,
                        index := i3,
                        bpp := i11,
                        colorspace := V4L2_COLORSPACE_SRGB,
                        fcc := i12,
                        flags := i15,
                        frame_base
                    })
              else
                let i21 ← pos1 - start_pos
                let frame_err ← lift (UScalar.hcast .I32 i21)
                ok (frame_err, self,
                  {
                    format
                      with
                      type_ := i1,
                      index := i3,
                      bpp := i11,
                      colorspace := V4L2_COLORSPACE_SRGB,
                      fcc := i12,
                      flags := i15,
                      frame_base
                  })
            else
              let i19 ← pos1 - start_pos
              let frame_err ← lift (UScalar.hcast .I32 i19)
              ok (frame_err, self,
                {
                  format
                    with
                    type_ := i1,
                    index := i3,
                    bpp := i11,
                    colorspace := V4L2_COLORSPACE_SRGB,
                    fcc := i12,
                    flags := i15,
                    frame_base
                })
    | 6#uscalar =>
      if start_buflen < 11#i32
      then
        let frame_err ← -. EINVAL
        ok (frame_err, self,
          { format with type_ := i1, index := i3, frame_base })
      else
        let i5 ← V4L2_PIX_FMT_MJPEG
        let i6 ← Slice.index_usize buf start_pos
        let i7 ← lift (UScalar.hcast .I32 i6)
        let buflen ← start_buflen - i7
        let i8 ← lift (UScalar.cast .Usize i6)
        let pos ← start_pos + i8
        if UVC_VS_FRAME_MJPEG != 0#u8
        then
          let (self1, i9, pos1, buflen1, frame_err) ←
            ParseState.uvc_parse_format_loop2 self dev_quirks i1 i3
              format.colorspace format.xfer_func format.ycbcr_enc i5
              UVC_FMT_FLAG_COMPRESSED format.nframes frame_base buf pos buflen
              UVC_VS_FRAME_MJPEG
          if frame_err < 0#i32
          then
            ok (frame_err, self1,
              {
                format
                  with
                  type_ := i1,
                  index := i3,
                  bpp := 0#u8,
                  fcc := i5,
                  flags := UVC_FMT_FLAG_COMPRESSED,
                  nframes := i9,
                  frame_base
              })
          else
            let (pos2, buflen2) ←
              if buflen1 > 2#i32
              then
                do
                let i10 ← pos1 + 1#usize
                let i11 ← Slice.index_usize buf i10
                if i11 = USB_DT_CS_INTERFACE
                then
                  let i12 ← pos1 + 2#usize
                  let i13 ← Slice.index_usize buf i12
                  if i13 = UVC_VS_STILL_IMAGE_FRAME
                  then
                    let i14 ← Slice.index_usize buf pos1
                    let i15 ← lift (UScalar.hcast .I32 i14)
                    let buflen3 ← buflen1 - i15
                    let i16 ← lift (UScalar.cast .Usize i14)
                    let pos3 ← pos1 + i16
                    ok (pos3, buflen3)
                  else ok (pos1, buflen1)
                else ok (pos1, buflen1)
              else ok (pos1, buflen1)
            if buflen2 > 2#i32
            then
              let i10 ← pos2 + 1#usize
              let i11 ← Slice.index_usize buf i10
              if i11 = USB_DT_CS_INTERFACE
              then
                let i12 ← pos2 + 2#usize
                let i13 ← Slice.index_usize buf i12
                if i13 = UVC_VS_COLORFORMAT
                then
                  if buflen2 < 6#i32
                  then
                    let frame_err1 ← -. EINVAL
                    ok (frame_err1, self1,
                      {
                        format
                          with
                          type_ := i1,
                          index := i3,
                          bpp := 0#u8,
                          fcc := i5,
                          flags := UVC_FMT_FLAG_COMPRESSED,
                          nframes := i9,
                          frame_base
                      })
                  else
                    let i14 ← pos2 + 3#usize
                    let i15 ← Slice.index_usize buf i14
                    let i16 ← uvc_colorspace i15
                    let i17 ← pos2 + 4#usize
                    let i18 ← Slice.index_usize buf i17
                    let i19 ← uvc_xfer_func i18
                    let i20 ← pos2 + 5#usize
                    let i21 ← Slice.index_usize buf i20
                    let i22 ← uvc_ycbcr_enc i21
                    let i23 ← Slice.index_usize buf pos2
                    let i24 ← lift (UScalar.hcast .I32 i23)
                    let _ ← buflen2 - i24
                    let i25 ← lift (UScalar.cast .Usize i23)
                    let pos3 ← pos2 + i25
                    let i26 ← pos3 - start_pos
                    let frame_err1 ← lift (UScalar.hcast .I32 i26)
                    ok (frame_err1, self1,
                      {
                        type_ := i1,
                        index := i3,
                        bpp := 0#u8,
                        colorspace := i16,
                        xfer_func := i19,
                        ycbcr_enc := i22,
                        fcc := i5,
                        flags := UVC_FMT_FLAG_COMPRESSED,
                        nframes := i9,
                        frame_base
                      })
                else
                  let i14 ← pos2 - start_pos
                  let frame_err1 ← lift (UScalar.hcast .I32 i14)
                  ok (frame_err1, self1,
                    {
                      format
                        with
                        type_ := i1,
                        index := i3,
                        bpp := 0#u8,
                        colorspace := V4L2_COLORSPACE_SRGB,
                        fcc := i5,
                        flags := UVC_FMT_FLAG_COMPRESSED,
                        nframes := i9,
                        frame_base
                    })
              else
                let i12 ← pos2 - start_pos
                let frame_err1 ← lift (UScalar.hcast .I32 i12)
                ok (frame_err1, self1,
                  {
                    format
                      with
                      type_ := i1,
                      index := i3,
                      bpp := 0#u8,
                      colorspace := V4L2_COLORSPACE_SRGB,
                      fcc := i5,
                      flags := UVC_FMT_FLAG_COMPRESSED,
                      nframes := i9,
                      frame_base
                  })
            else
              let i10 ← pos2 - start_pos
              let frame_err1 ← lift (UScalar.hcast .I32 i10)
              ok (frame_err1, self1,
                {
                  format
                    with
                    type_ := i1,
                    index := i3,
                    bpp := 0#u8,
                    colorspace := V4L2_COLORSPACE_SRGB,
                    fcc := i5,
                    flags := UVC_FMT_FLAG_COMPRESSED,
                    nframes := i9,
                    frame_base
                })
        else
          let (pos1, buflen1) ←
            if buflen > 2#i32
            then
              do
              let i9 ← pos + 1#usize
              let i10 ← Slice.index_usize buf i9
              if i10 = USB_DT_CS_INTERFACE
              then
                let i11 ← pos + 2#usize
                let i12 ← Slice.index_usize buf i11
                if i12 = UVC_VS_STILL_IMAGE_FRAME
                then
                  let i13 ← Slice.index_usize buf pos
                  let i14 ← lift (UScalar.hcast .I32 i13)
                  let buflen2 ← buflen - i14
                  let i15 ← lift (UScalar.cast .Usize i13)
                  let pos2 ← pos + i15
                  ok (pos2, buflen2)
                else ok (pos, buflen)
              else ok (pos, buflen)
            else ok (pos, buflen)
          if buflen1 > 2#i32
          then
            let i9 ← pos1 + 1#usize
            let i10 ← Slice.index_usize buf i9
            if i10 = USB_DT_CS_INTERFACE
            then
              let i11 ← pos1 + 2#usize
              let i12 ← Slice.index_usize buf i11
              if i12 = UVC_VS_COLORFORMAT
              then
                if buflen1 < 6#i32
                then
                  let frame_err ← -. EINVAL
                  ok (frame_err, self,
                    {
                      format
                        with
                        type_ := i1,
                        index := i3,
                        bpp := 0#u8,
                        fcc := i5,
                        flags := UVC_FMT_FLAG_COMPRESSED,
                        frame_base
                    })
                else
                  let i13 ← pos1 + 3#usize
                  let i14 ← Slice.index_usize buf i13
                  let i15 ← uvc_colorspace i14
                  let i16 ← pos1 + 4#usize
                  let i17 ← Slice.index_usize buf i16
                  let i18 ← uvc_xfer_func i17
                  let i19 ← pos1 + 5#usize
                  let i20 ← Slice.index_usize buf i19
                  let i21 ← uvc_ycbcr_enc i20
                  let i22 ← Slice.index_usize buf pos1
                  let i23 ← lift (UScalar.hcast .I32 i22)
                  let _ ← buflen1 - i23
                  let i24 ← lift (UScalar.cast .Usize i22)
                  let pos2 ← pos1 + i24
                  let i25 ← pos2 - start_pos
                  let frame_err ← lift (UScalar.hcast .I32 i25)
                  ok (frame_err, self,
                    {
                      format
                        with
                        type_ := i1,
                        index := i3,
                        bpp := 0#u8,
                        colorspace := i15,
                        xfer_func := i18,
                        ycbcr_enc := i21,
                        fcc := i5,
                        flags := UVC_FMT_FLAG_COMPRESSED,
                        frame_base
                    })
              else
                let i13 ← pos1 - start_pos
                let frame_err ← lift (UScalar.hcast .I32 i13)
                ok (frame_err, self,
                  {
                    format
                      with
                      type_ := i1,
                      index := i3,
                      bpp := 0#u8,
                      colorspace := V4L2_COLORSPACE_SRGB,
                      fcc := i5,
                      flags := UVC_FMT_FLAG_COMPRESSED,
                      frame_base
                  })
            else
              let i11 ← pos1 - start_pos
              let frame_err ← lift (UScalar.hcast .I32 i11)
              ok (frame_err, self,
                {
                  format
                    with
                    type_ := i1,
                    index := i3,
                    bpp := 0#u8,
                    colorspace := V4L2_COLORSPACE_SRGB,
                    fcc := i5,
                    flags := UVC_FMT_FLAG_COMPRESSED,
                    frame_base
                })
          else
            let i9 ← pos1 - start_pos
            let frame_err ← lift (UScalar.hcast .I32 i9)
            ok (frame_err, self,
              {
                format
                  with
                  type_ := i1,
                  index := i3,
                  bpp := 0#u8,
                  colorspace := V4L2_COLORSPACE_SRGB,
                  fcc := i5,
                  flags := UVC_FMT_FLAG_COMPRESSED,
                  frame_base
              })
    | 12#uscalar =>
      if start_buflen < 9#i32
      then
        let frame_err ← -. EINVAL
        ok (frame_err, self,
          { format with type_ := i1, index := i3, frame_base })
      else
        let i5 ← start_pos + 8#usize
        let i6 ← Slice.index_usize buf i5
        let i7 ← lift (i6 &&& 127#u8)
        if i7 > 2#u8
        then
          let frame_err ← -. EINVAL
          ok (frame_err, self,
            { format with type_ := i1, index := i3, frame_base })
        else
          let i8 ← V4L2_PIX_FMT_DV
          let i9 ← lift (UVC_FMT_FLAG_COMPRESSED ||| UVC_FMT_FLAG_STREAM)
          let frame ← UvcFrame.Insts.CoreDefaultDefault.default
          let i10 ← lift (UScalar.hcast .I32 self.interval_cursor)
          let (_, index_mut_back) ←
            alloc.vec.Vec.index_mut (core.slice.index.SliceIndexUsizeSlice
              Std.U32) self.intervals self.interval_cursor
          let i11 ← self.interval_cursor + 1#usize
          let (_, index_mut_back1) ←
            alloc.vec.Vec.index_mut (core.slice.index.SliceIndexUsizeSlice
              UvcFrame) self.frames frame_base
          let i12 ← Slice.index_usize buf start_pos
          let i13 ← lift (UScalar.hcast .I32 i12)
          let buflen ← start_buflen - i13
          let i14 ← lift (UScalar.cast .Usize i12)
          let pos ← start_pos + i14
          if 0#u8 != 0#u8
          then
            let v :=
              index_mut_back1
                {
                  frame
                    with
                    b_frame_interval_type := 1#u8,
                    dw_default_frame_interval := 1#u32,
                    interval_base := i10
                }
            let v1 := index_mut_back 1#u32
            let (self1, i15, pos1, buflen1, frame_err) ←
              ParseState.uvc_parse_format_loop3
                { frames := v, intervals := v1, interval_cursor := i11 }
                dev_quirks i1 i3 format.colorspace format.xfer_func
                format.ycbcr_enc i8 i9 1#u32 frame_base buf pos buflen
            if frame_err < 0#i32
            then
              ok (frame_err, self1,
                {
                  format
                    with
                    type_ := i1,
                    index := i3,
                    bpp := 0#u8,
                    fcc := i8,
                    flags := i9,
                    nframes := i15,
                    frame_base
                })
            else
              let (pos2, buflen2) ←
                if buflen1 > 2#i32
                then
                  do
                  let i16 ← pos1 + 1#usize
                  let i17 ← Slice.index_usize buf i16
                  if i17 = USB_DT_CS_INTERFACE
                  then
                    let i18 ← pos1 + 2#usize
                    let i19 ← Slice.index_usize buf i18
                    if i19 = UVC_VS_STILL_IMAGE_FRAME
                    then
                      let i20 ← Slice.index_usize buf pos1
                      let i21 ← lift (UScalar.hcast .I32 i20)
                      let buflen3 ← buflen1 - i21
                      let i22 ← lift (UScalar.cast .Usize i20)
                      let pos3 ← pos1 + i22
                      ok (pos3, buflen3)
                    else ok (pos1, buflen1)
                  else ok (pos1, buflen1)
                else ok (pos1, buflen1)
              if buflen2 > 2#i32
              then
                let i16 ← pos2 + 1#usize
                let i17 ← Slice.index_usize buf i16
                if i17 = USB_DT_CS_INTERFACE
                then
                  let i18 ← pos2 + 2#usize
                  let i19 ← Slice.index_usize buf i18
                  if i19 = UVC_VS_COLORFORMAT
                  then
                    if buflen2 < 6#i32
                    then
                      let frame_err1 ← -. EINVAL
                      ok (frame_err1, self1,
                        {
                          format
                            with
                            type_ := i1,
                            index := i3,
                            bpp := 0#u8,
                            fcc := i8,
                            flags := i9,
                            nframes := i15,
                            frame_base
                        })
                    else
                      let i20 ← pos2 + 3#usize
                      let i21 ← Slice.index_usize buf i20
                      let i22 ← uvc_colorspace i21
                      let i23 ← pos2 + 4#usize
                      let i24 ← Slice.index_usize buf i23
                      let i25 ← uvc_xfer_func i24
                      let i26 ← pos2 + 5#usize
                      let i27 ← Slice.index_usize buf i26
                      let i28 ← uvc_ycbcr_enc i27
                      let i29 ← Slice.index_usize buf pos2
                      let i30 ← lift (UScalar.hcast .I32 i29)
                      let _ ← buflen2 - i30
                      let i31 ← lift (UScalar.cast .Usize i29)
                      let pos3 ← pos2 + i31
                      let i32 ← pos3 - start_pos
                      let frame_err1 ← lift (UScalar.hcast .I32 i32)
                      ok (frame_err1, self1,
                        {
                          type_ := i1,
                          index := i3,
                          bpp := 0#u8,
                          colorspace := i22,
                          xfer_func := i25,
                          ycbcr_enc := i28,
                          fcc := i8,
                          flags := i9,
                          nframes := i15,
                          frame_base
                        })
                  else
                    let i20 ← pos2 - start_pos
                    let frame_err1 ← lift (UScalar.hcast .I32 i20)
                    ok (frame_err1, self1,
                      {
                        format
                          with
                          type_ := i1,
                          index := i3,
                          bpp := 0#u8,
                          colorspace := V4L2_COLORSPACE_SRGB,
                          fcc := i8,
                          flags := i9,
                          nframes := i15,
                          frame_base
                      })
                else
                  let i18 ← pos2 - start_pos
                  let frame_err1 ← lift (UScalar.hcast .I32 i18)
                  ok (frame_err1, self1,
                    {
                      format
                        with
                        type_ := i1,
                        index := i3,
                        bpp := 0#u8,
                        colorspace := V4L2_COLORSPACE_SRGB,
                        fcc := i8,
                        flags := i9,
                        nframes := i15,
                        frame_base
                    })
              else
                let i16 ← pos2 - start_pos
                let frame_err1 ← lift (UScalar.hcast .I32 i16)
                ok (frame_err1, self1,
                  {
                    format
                      with
                      type_ := i1,
                      index := i3,
                      bpp := 0#u8,
                      colorspace := V4L2_COLORSPACE_SRGB,
                      fcc := i8,
                      flags := i9,
                      nframes := i15,
                      frame_base
                  })
          else
            let (v, v1, pos1, buflen1) ←
              if buflen > 2#i32
              then
                do
                let i15 ← pos + 1#usize
                let i16 ← Slice.index_usize buf i15
                if i16 = USB_DT_CS_INTERFACE
                then
                  let i17 ← pos + 2#usize
                  let i18 ← Slice.index_usize buf i17
                  if i18 = UVC_VS_STILL_IMAGE_FRAME
                  then
                    let i19 ← Slice.index_usize buf pos
                    let i20 ← lift (UScalar.hcast .I32 i19)
                    let buflen2 ← buflen - i20
                    let i21 ← lift (UScalar.cast .Usize i19)
                    let pos2 ← pos + i21
                    let v2 :=
                      index_mut_back1
                        {
                          frame
                            with
                            b_frame_interval_type := 1#u8,
                            dw_default_frame_interval := 1#u32,
                            interval_base := i10
                        }
                    let v3 := index_mut_back 1#u32
                    ok (v2, v3, pos2, buflen2)
                  else
                    let v2 :=
                      index_mut_back1
                        {
                          frame
                            with
                            b_frame_interval_type := 1#u8,
                            dw_default_frame_interval := 1#u32,
                            interval_base := i10
                        }
                    let v3 := index_mut_back 1#u32
                    ok (v2, v3, pos, buflen)
                else
                  let v2 :=
                    index_mut_back1
                      {
                        frame
                          with
                          b_frame_interval_type := 1#u8,
                          dw_default_frame_interval := 1#u32,
                          interval_base := i10
                      }
                  let v3 := index_mut_back 1#u32
                  ok (v2, v3, pos, buflen)
              else
                let v2 :=
                  index_mut_back1
                    {
                      frame
                        with
                        b_frame_interval_type := 1#u8,
                        dw_default_frame_interval := 1#u32,
                        interval_base := i10
                    }
                let v3 := index_mut_back 1#u32
                ok (v2, v3, pos, buflen)
            if buflen1 > 2#i32
            then
              let i15 ← pos1 + 1#usize
              let i16 ← Slice.index_usize buf i15
              if i16 = USB_DT_CS_INTERFACE
              then
                let i17 ← pos1 + 2#usize
                let i18 ← Slice.index_usize buf i17
                if i18 = UVC_VS_COLORFORMAT
                then
                  if buflen1 < 6#i32
                  then
                    let frame_err ← -. EINVAL
                    ok (frame_err,
                      { frames := v, intervals := v1, interval_cursor := i11 },
                      {
                        format
                          with
                          type_ := i1,
                          index := i3,
                          bpp := 0#u8,
                          fcc := i8,
                          flags := i9,
                          nframes := 1#u32,
                          frame_base
                      })
                  else
                    let i19 ← pos1 + 3#usize
                    let i20 ← Slice.index_usize buf i19
                    let i21 ← uvc_colorspace i20
                    let i22 ← pos1 + 4#usize
                    let i23 ← Slice.index_usize buf i22
                    let i24 ← uvc_xfer_func i23
                    let i25 ← pos1 + 5#usize
                    let i26 ← Slice.index_usize buf i25
                    let i27 ← uvc_ycbcr_enc i26
                    let i28 ← Slice.index_usize buf pos1
                    let i29 ← lift (UScalar.hcast .I32 i28)
                    let _ ← buflen1 - i29
                    let i30 ← lift (UScalar.cast .Usize i28)
                    let pos2 ← pos1 + i30
                    let i31 ← pos2 - start_pos
                    let frame_err ← lift (UScalar.hcast .I32 i31)
                    ok (frame_err,
                      { frames := v, intervals := v1, interval_cursor := i11 },
                      {
                        type_ := i1,
                        index := i3,
                        bpp := 0#u8,
                        colorspace := i21,
                        xfer_func := i24,
                        ycbcr_enc := i27,
                        fcc := i8,
                        flags := i9,
                        nframes := 1#u32,
                        frame_base
                      })
                else
                  let i19 ← pos1 - start_pos
                  let frame_err ← lift (UScalar.hcast .I32 i19)
                  ok (frame_err,
                    { frames := v, intervals := v1, interval_cursor := i11 },
                    {
                      format
                        with
                        type_ := i1,
                        index := i3,
                        bpp := 0#u8,
                        colorspace := V4L2_COLORSPACE_SRGB,
                        fcc := i8,
                        flags := i9,
                        nframes := 1#u32,
                        frame_base
                    })
              else
                let i17 ← pos1 - start_pos
                let frame_err ← lift (UScalar.hcast .I32 i17)
                ok (frame_err,
                  { frames := v, intervals := v1, interval_cursor := i11 },
                  {
                    format
                      with
                      type_ := i1,
                      index := i3,
                      bpp := 0#u8,
                      colorspace := V4L2_COLORSPACE_SRGB,
                      fcc := i8,
                      flags := i9,
                      nframes := 1#u32,
                      frame_base
                  })
            else
              let i15 ← pos1 - start_pos
              let frame_err ← lift (UScalar.hcast .I32 i15)
              ok (frame_err,
                { frames := v, intervals := v1, interval_cursor := i11 },
                {
                  format
                    with
                    type_ := i1,
                    index := i3,
                    bpp := 0#u8,
                    colorspace := V4L2_COLORSPACE_SRGB,
                    fcc := i8,
                    flags := i9,
                    nframes := 1#u32,
                    frame_base
                })
    | 10#uscalar =>
      let frame_err ← -. EINVAL
      ok (frame_err, self,
        { format with type_ := i1, index := i3, frame_base })
    | 18#uscalar =>
      let frame_err ← -. EINVAL
      ok (frame_err, self,
        { format with type_ := i1, index := i3, frame_base })
    | _ =>
      let frame_err ← -. EINVAL
      ok (frame_err, self,
        { format with type_ := i1, index := i3, frame_base })

/-- [uvc_verify::{uvc_verify::UvcFrameOut}::zeroed]:
    Source: 'src/lib.rs', lines 880:4-893:5 -/
def UvcFrameOut.zeroed : Result UvcFrameOut := do
  ok
    {
      b_frame_index := 0#u8,
      bm_capabilities := 0#u8,
      w_width := 0#u16,
      w_height := 0#u16,
      dw_min_bit_rate := 0#u32,
      dw_max_bit_rate := 0#u32,
      dw_max_video_frame_buffer_size := 0#u32,
      b_frame_interval_type := 0#u8,
      dw_default_frame_interval := 0#u32,
      interval_base := 0#i32
    }

/-- [uvc_verify::{uvc_verify::UvcFormatOut}::zeroed]:
    Source: 'src/lib.rs', lines 864:4-877:5 -/
def UvcFormatOut.zeroed : Result UvcFormatOut := do
  ok
    {
      type_ := 0#u8,
      index := 0#u8,
      bpp := 0#u8,
      colorspace := 0#i32,
      xfer_func := 0#i32,
      ycbcr_enc := 0#i32,
      fcc := 0#u32,
      flags := 0#u32,
      nframes := 0#u32,
      frame_base := 0#i32
    }

/-- [uvc_verify::parse]: loop body 0:
    Source: 'src/lib.rs', lines 1:0-755:9
    Visibility: public -/
@[rust_loop_body]
def parse_loop0.body
  (buf : Slice Std.U8) (nformats : Std.U32) (nframes : Std.U32)
  (nintervals : Std.U32) (pos : Std.Usize) (buflen : Std.I32) :
  Result (ControlFlow (Std.U32 × Std.U32 × Std.U32 × Std.Usize × Std.I32)
    (Std.U32 × Std.U32 × Std.U32))
  := do
  if buflen > 2#i32
  then
    let i ← pos + 1#usize
    let i1 ← Slice.index_usize buf i
    if i1 = USB_DT_CS_INTERFACE
    then
      let i2 ← pos + 2#usize
      let i3 ← Slice.index_usize buf i2
      let (nformats1, nframes1, nintervals1) ←
        match i3 with
        | 4#uscalar =>
          do
          let nformats2 ← nformats + 1#u32
          ok (nformats2, nframes, nintervals)
        | 6#uscalar =>
          do
          let nformats2 ← nformats + 1#u32
          ok (nformats2, nframes, nintervals)
        | 16#uscalar =>
          do
          let nformats2 ← nformats + 1#u32
          ok (nformats2, nframes, nintervals)
        | 12#uscalar =>
          do
          let nformats2 ← nformats + 1#u32
          let nframes2 ← nframes + 1#u32
          let nintervals2 ← nintervals + 1#u32
          ok (nformats2, nframes2, nintervals2)
        | 10#uscalar => ok (nformats, nframes, nintervals)
        | 18#uscalar => ok (nformats, nframes, nintervals)
        | 5#uscalar =>
          do
          let nframes2 ← nframes + 1#u32
          let i4 ←
            if buflen > 25#i32
            then
              do
              let i5 ← pos + 25#usize
              let i6 ← Slice.index_usize buf i5
              let i7 ←
                if i6 != 0#u8
                then
                  do
                  let i8 ← Slice.index_usize buf i5
                  ok (UScalar.cast .U32 i8)
                else ok 3#u32
              nintervals + i7
            else ok nintervals
          ok (nformats, nframes2, i4)
        | 7#uscalar =>
          do
          let nframes2 ← nframes + 1#u32
          let i4 ←
            if buflen > 25#i32
            then
              do
              let i5 ← pos + 25#usize
              let i6 ← Slice.index_usize buf i5
              let i7 ←
                if i6 != 0#u8
                then
                  do
                  let i8 ← Slice.index_usize buf i5
                  ok (UScalar.cast .U32 i8)
                else ok 3#u32
              nintervals + i7
            else ok nintervals
          ok (nformats, nframes2, i4)
        | 17#uscalar =>
          do
          let nframes2 ← nframes + 1#u32
          let i4 ←
            if buflen > 21#i32
            then
              do
              let i5 ← pos + 21#usize
              let i6 ← Slice.index_usize buf i5
              let i7 ←
                if i6 != 0#u8
                then
                  do
                  let i8 ← Slice.index_usize buf i5
                  ok (UScalar.cast .U32 i8)
                else ok 3#u32
              nintervals + i7
            else ok nintervals
          ok (nformats, nframes2, i4)
        | _ => ok (nformats, nframes, nintervals)
      let i4 ← Slice.index_usize buf pos
      let i5 ← lift (UScalar.hcast .I32 i4)
      let buflen1 ← buflen - i5
      let i6 ← lift (UScalar.cast .Usize i4)
      let pos1 ← pos + i6
      ok (cont (nformats1, nframes1, nintervals1, pos1, buflen1))
    else ok (done (nformats, nframes, nintervals))
  else ok (done (nformats, nframes, nintervals))

/-- [uvc_verify::parse]: loop 0:
    Source: 'src/lib.rs', lines 1:0-755:9
    Visibility: public -/
@[rust_loop]
def parse_loop0
  (buf : Slice Std.U8) (nformats : Std.U32) (nframes : Std.U32)
  (nintervals : Std.U32) (pos : Std.Usize) (buflen : Std.I32) :
  Result (Std.U32 × Std.U32 × Std.U32)
  := do
  loop
    (fun (nformats1, nframes1, nintervals1, pos1, buflen1) => parse_loop0.body
      buf nformats1 nframes1 nintervals1 pos1 buflen1)
    (nformats, nframes, nintervals, pos, buflen)

/-- [uvc_verify::parse]: loop body 1:
    Source: 'src/lib.rs', lines 1:0-813:5
    Visibility: public -/
@[rust_loop_body]
def parse_loop1.body
  (buf : Slice Std.U8) (quirks : Std.U32) (state : ParseState)
  (formats : alloc.vec.Vec UvcFormat) (pos : Std.Usize) (buflen : Std.I32)
  (frame_base : Std.Usize) (ret : Std.I32) :
  Result (ControlFlow (ParseState × (alloc.vec.Vec UvcFormat) × Std.Usize ×
    Std.I32 × Std.Usize × Std.I32) (ParseState × (alloc.vec.Vec UvcFormat)
    × Std.Usize × Std.I32))
  := do
  if buflen > 2#i32
  then
    let i ← pos + 1#usize
    let i1 ← Slice.index_usize buf i
    if i1 = USB_DT_CS_INTERFACE
    then
      let i2 ← pos + 2#usize
      let i3 ← Slice.index_usize buf i2
      match i3 with
      | 4#uscalar =>
        let format ← UvcFormat.Insts.CoreDefaultDefault.default
        let (ret1, state1, format1) ←
          ParseState.uvc_parse_format state quirks format frame_base buf pos
            buflen
        if ret1 < 0#i32
        then ok (done (state1, formats, frame_base, ret1))
        else
          if ret1 = 0#i32
          then
            let i4 ← Slice.index_usize buf pos
            let i5 ← lift (UScalar.hcast .I32 i4)
            let buflen1 ← buflen - i5
            let i6 ← lift (UScalar.cast .Usize i4)
            let pos1 ← pos + i6
            ok (cont (state1, formats, pos1, buflen1, frame_base, ret1))
          else
            let i4 ← lift (UScalar.cast .Usize format1.nframes)
            let frame_base1 ← frame_base + i4
            let formats1 ← alloc.vec.Vec.push formats format1
            let buflen1 ← buflen - ret1
            let i5 ← lift (IScalar.hcast .Usize ret1)
            let pos1 ← pos + i5
            ok (cont (state1, formats1, pos1, buflen1, frame_base1, ret1))
      | 6#uscalar =>
        let format ← UvcFormat.Insts.CoreDefaultDefault.default
        let (ret1, state1, format1) ←
          ParseState.uvc_parse_format state quirks format frame_base buf pos
            buflen
        if ret1 < 0#i32
        then ok (done (state1, formats, frame_base, ret1))
        else
          if ret1 = 0#i32
          then
            let i4 ← Slice.index_usize buf pos
            let i5 ← lift (UScalar.hcast .I32 i4)
            let buflen1 ← buflen - i5
            let i6 ← lift (UScalar.cast .Usize i4)
            let pos1 ← pos + i6
            ok (cont (state1, formats, pos1, buflen1, frame_base, ret1))
          else
            let i4 ← lift (UScalar.cast .Usize format1.nframes)
            let frame_base1 ← frame_base + i4
            let formats1 ← alloc.vec.Vec.push formats format1
            let buflen1 ← buflen - ret1
            let i5 ← lift (IScalar.hcast .Usize ret1)
            let pos1 ← pos + i5
            ok (cont (state1, formats1, pos1, buflen1, frame_base1, ret1))
      | 12#uscalar =>
        let format ← UvcFormat.Insts.CoreDefaultDefault.default
        let (ret1, state1, format1) ←
          ParseState.uvc_parse_format state quirks format frame_base buf pos
            buflen
        if ret1 < 0#i32
        then ok (done (state1, formats, frame_base, ret1))
        else
          if ret1 = 0#i32
          then
            let i4 ← Slice.index_usize buf pos
            let i5 ← lift (UScalar.hcast .I32 i4)
            let buflen1 ← buflen - i5
            let i6 ← lift (UScalar.cast .Usize i4)
            let pos1 ← pos + i6
            ok (cont (state1, formats, pos1, buflen1, frame_base, ret1))
          else
            let i4 ← lift (UScalar.cast .Usize format1.nframes)
            let frame_base1 ← frame_base + i4
            let formats1 ← alloc.vec.Vec.push formats format1
            let buflen1 ← buflen - ret1
            let i5 ← lift (IScalar.hcast .Usize ret1)
            let pos1 ← pos + i5
            ok (cont (state1, formats1, pos1, buflen1, frame_base1, ret1))
      | 16#uscalar =>
        let format ← UvcFormat.Insts.CoreDefaultDefault.default
        let (ret1, state1, format1) ←
          ParseState.uvc_parse_format state quirks format frame_base buf pos
            buflen
        if ret1 < 0#i32
        then ok (done (state1, formats, frame_base, ret1))
        else
          if ret1 = 0#i32
          then
            let i4 ← Slice.index_usize buf pos
            let i5 ← lift (UScalar.hcast .I32 i4)
            let buflen1 ← buflen - i5
            let i6 ← lift (UScalar.cast .Usize i4)
            let pos1 ← pos + i6
            ok (cont (state1, formats, pos1, buflen1, frame_base, ret1))
          else
            let i4 ← lift (UScalar.cast .Usize format1.nframes)
            let frame_base1 ← frame_base + i4
            let formats1 ← alloc.vec.Vec.push formats format1
            let buflen1 ← buflen - ret1
            let i5 ← lift (IScalar.hcast .Usize ret1)
            let pos1 ← pos + i5
            ok (cont (state1, formats1, pos1, buflen1, frame_base1, ret1))
      | _ =>
        let i4 ← Slice.index_usize buf pos
        let i5 ← lift (UScalar.hcast .I32 i4)
        let buflen1 ← buflen - i5
        let i6 ← lift (UScalar.cast .Usize i4)
        let pos1 ← pos + i6
        ok (cont (state, formats, pos1, buflen1, frame_base, ret))
    else ok (done (state, formats, frame_base, ret))
  else ok (done (state, formats, frame_base, ret))

/-- [uvc_verify::parse]: loop 1:
    Source: 'src/lib.rs', lines 1:0-813:5
    Visibility: public -/
@[rust_loop]
def parse_loop1
  (buf : Slice Std.U8) (quirks : Std.U32) (state : ParseState)
  (formats : alloc.vec.Vec UvcFormat) (pos : Std.Usize) (buflen : Std.I32)
  (frame_base : Std.Usize) (ret : Std.I32) :
  Result (ParseState × (alloc.vec.Vec UvcFormat) × Std.Usize × Std.I32)
  := do
  loop
    (fun (state1, formats1, pos1, buflen1, frame_base1, ret1) =>
      parse_loop1.body buf quirks state1 formats1 pos1 buflen1 frame_base1
      ret1)
    (state, formats, pos, buflen, frame_base, ret)

/-- [uvc_verify::parse]: loop body 2:
    Source: 'src/lib.rs', lines 829:4-842:5
    Visibility: public -/
@[rust_loop_body]
def parse_loop2.body
  (iter : core.iter.adapters.enumerate.Enumerate (core.iter.adapters.take.Take
  (core.slice.iter.Iter UvcFormat))) (a : Array UvcFormatOut 64#usize) :
  Result (ControlFlow ((core.iter.adapters.enumerate.Enumerate
    (core.iter.adapters.take.Take (core.slice.iter.Iter UvcFormat))) × (Array
    UvcFormatOut 64#usize)) (Array UvcFormatOut 64#usize))
  := do
  let (o, iter1) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorTake
      (core.iter.traits.iterator.IteratorSliceIter UvcFormat)) iter
  match o with
  | none => ok (done a)
  | some p =>
    let (i, f) := p
    let i1 ← lift (UScalar.hcast .I32 f.frame_base)
    let a1 ←
      Array.update a i
        ({
           type_ := f.type_,
           index := f.index,
           bpp := f.bpp,
           colorspace := f.colorspace,
           xfer_func := f.xfer_func,
           ycbcr_enc := f.ycbcr_enc,
           fcc := f.fcc,
           flags := f.flags,
           nframes := f.nframes,
           frame_base := i1
         } : UvcFormatOut)
    ok (cont (iter1, a1))

/-- [uvc_verify::parse]: loop 2:
    Source: 'src/lib.rs', lines 829:4-842:5
    Visibility: public -/
@[rust_loop]
def parse_loop2
  (iter : core.iter.adapters.enumerate.Enumerate (core.iter.adapters.take.Take
  (core.slice.iter.Iter UvcFormat))) (a : Array UvcFormatOut 64#usize) :
  Result (Array UvcFormatOut 64#usize)
  := do
  loop
    (fun (iter1, a1) => parse_loop2.body iter1 a1)
    (iter, a)

/-- [uvc_verify::parse]: loop body 3:
    Source: 'src/lib.rs', lines 843:4-857:5
    Visibility: public -/
@[rust_loop_body]
def parse_loop3.body
  (v : alloc.vec.Vec UvcFrame) (iter : core.ops.range.Range Std.Usize)
  (a : Array UvcFrameOut 256#usize) :
  Result (ControlFlow ((core.ops.range.Range Std.Usize) × (Array UvcFrameOut
    256#usize)) (Array UvcFrameOut 256#usize))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done a)
  | some i =>
    let f ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice UvcFrame) v i
    let a1 ←
      Array.update a i
        ({
           b_frame_index := f.b_frame_index,
           bm_capabilities := f.bm_capabilities,
           w_width := f.w_width,
           w_height := f.w_height,
           dw_min_bit_rate := f.dw_min_bit_rate,
           dw_max_bit_rate := f.dw_max_bit_rate,
           dw_max_video_frame_buffer_size := f.dw_max_video_frame_buffer_size,
           b_frame_interval_type := f.b_frame_interval_type,
           dw_default_frame_interval := f.dw_default_frame_interval,
           interval_base := f.interval_base
         } : UvcFrameOut)
    ok (cont (iter1, a1))

/-- [uvc_verify::parse]: loop 3:
    Source: 'src/lib.rs', lines 843:4-857:5
    Visibility: public -/
@[rust_loop]
def parse_loop3
  (iter : core.ops.range.Range Std.Usize) (a : Array UvcFrameOut 256#usize)
  (v : alloc.vec.Vec UvcFrame) :
  Result (Array UvcFrameOut 256#usize)
  := do
  loop
    (fun (iter1, a1) => parse_loop3.body v iter1 a1)
    (iter, a)

/-- [uvc_verify::parse]:
    Source: 'src/lib.rs', lines 696:0-861:1
    Visibility: public -/
def parse
  (buf : Slice Std.U8) (quirks : Std.U32) (out : UvcParseResult) :
  Result (Std.I32 × UvcParseResult)
  := do
  let ufo ← UvcFormatOut.zeroed
  let a := Array.repeat 64#usize ufo
  let ufo1 ← UvcFrameOut.zeroed
  let a1 := Array.repeat 256#usize ufo1
  let a2 := Array.repeat 1024#usize 0#u32
  let i := Slice.len buf
  let total ← lift (UScalar.hcast .I32 i)
  let (nformats, nframes, nintervals) ←
    parse_loop0 buf 0#u32 0#u32 0#u32 0#usize total
  if nformats = 0#u32
  then
    let i1 ← -. EINVAL
    ok (i1,
      {
        ret := i1,
        nformats_alloc := 0#u32,
        nframes_alloc := 0#u32,
        nintervals_alloc := 0#u32,
        nformats := 0#u32,
        nframes := 0#u32,
        nintervals := 0#u32,
        formats := a,
        frames := a1,
        intervals := a2
      })
  else
    let i1 ← lift (UScalar.cast .Usize nformats)
    let i2 ← lift (UScalar.cast .Usize core.num.U32.MAX)
    if i1 > i2
    then
      let i3 ← -. ENOMEM
      ok (i3,
        {
          ret := i3,
          nformats_alloc := 0#u32,
          nframes_alloc := 0#u32,
          nintervals_alloc := 0#u32,
          nformats := 0#u32,
          nframes := 0#u32,
          nintervals := 0#u32,
          formats := a,
          frames := a1,
          intervals := a2
        })
    else
      let uf ← UvcFrame.Insts.CoreDefaultDefault.default
      let i3 ← lift (UScalar.cast .Usize nframes)
      let v ← alloc.vec.from_elem UvcFrame.Insts.CoreCloneClone uf i3
      let i4 ← lift (UScalar.cast .Usize nintervals)
      let v1 ← alloc.vec.from_elem core.clone.CloneU32 0#u32 i4
      let i5 ← lift (UScalar.cast .Usize nformats)
      let formats := alloc.vec.Vec.with_capacity UvcFormat i5
      let ret ← -. EINVAL
      let (state, formats1, frame_base, ret1) ←
        parse_loop1 buf quirks
          { frames := v, intervals := v1, interval_cursor := 0#usize } formats
          0#usize total 0#usize ret
      let ret2 ←
        if ret1 >= 0#i32
        then let i6 := alloc.vec.Vec.len formats1
             ok (UScalar.hcast .I32 i6)
        else ok ret1
      let i6 := alloc.vec.Vec.len formats1
      let nf ← lift (core.cmp.impls.OrdUsize.min i6 UVC_MAX_FORMATS)
      let total_frames ←
        lift (core.cmp.impls.OrdUsize.min frame_base UVC_MAX_FRAMES)
      let total_intervals ←
        lift (core.cmp.impls.OrdUsize.min state.interval_cursor
          UVC_MAX_INTERVALS)
      let i7 ← lift (UScalar.cast .U32 nf)
      let i8 ← lift (UScalar.cast .U32 total_frames)
      let i9 ← lift (UScalar.cast .U32 total_intervals)
      let s := alloc.vec.Vec.deref formats1
      let i10 ← core.slice.Slice.iter s
      let t ← core.slice.iter.IteratorSliceIter.take i10 nf
      let iter ←
        core.iter.adapters.take.IteratorTake.enumerate
          (core.iter.traits.iterator.IteratorSliceIter UvcFormat) t
      let a3 ← parse_loop2 iter a
      let a4 ←
        parse_loop3 { start := 0#usize, «end» := total_frames } a1
          state.frames
      let (s1, index_mut_back) ←
        core.array.Array.index_mut (core.ops.index.IndexMutSlice
          (core.slice.index.SliceIndexRangeToUsizeSlice Std.U32)) a2
          { «end» := total_intervals }
      let s2 ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexRangeToUsizeSlice
          Std.U32) state.intervals { «end» := total_intervals }
      let s3 ← core.slice.Slice.copy_from_slice core.marker.CopyU32 s1 s2
      let a5 := index_mut_back s3
      ok (ret2,
        {
          ret := ret2,
          nformats_alloc := nformats,
          nframes_alloc := nframes,
          nintervals_alloc := nintervals,
          nformats := i7,
          nframes := i8,
          nintervals := i9,
          formats := a3,
          frames := a4,
          intervals := a5
        })

end uvc_verify
